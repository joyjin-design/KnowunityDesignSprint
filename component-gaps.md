# Component gaps

Things built inline during a screen build because Storybook had no component for them. When the same thing is needed on a second screen, it gets built as a real component instead, and both lines are marked `→ promoted to <Name>`. Items `SPEC.md` says to build inline stay inline.

- Knowie's speech bubble holding the question (surface fill, left-pointing tail), plus the flat shadow under the mascot — for 1 Verdict sheet — inline in app/screens/VerdictScreen.tsx. `SPEC.md` screen 10 says to build the bubble inline, not as a component, so it isn't promoted when the loop screen needs it. Second occurrence: 5 Mic-off sheet, inline again in app/screens/MicOffScreen.tsx (reconstructing the loop's Idle look behind the sheet) — still not promoted, same exception. Third occurrence: 7 Why? sheet, inline again in app/screens/WhyScreen.tsx (same behind-the-sheet reconstruction, transcript included) — still not promoted, same exception. Fourth occurrence: 10 the loop itself, inline in app/screens/LoopScreen.tsx — the screen `SPEC.md` names as the inline exception, so this is its own copy, not a reconstruction of someone else's.
- Static XP chip "⚡2" (Phosphor Lightning and a label in `accent/blue/onSubtle`, no fill) — for 1 Verdict sheet — inline in app/screens/VerdictScreen.tsx. `Chip`'s colours (Primary, pro) don't cover it, and Figma draws it as a plain frame; whether it should be `Chip` is `SPEC.md` Open #8. Second occurrence: 5 Mic-off sheet, inline again in app/screens/MicOffScreen.tsx, same reasoning. Third occurrence: 7 Why? sheet, inline again in app/screens/WhyScreen.tsx. Fourth occurrence: 10 the loop itself, inline in app/screens/LoopScreen.tsx.
- Mascot thinking-loop animation (Processing: a small transform-only vertical bob on `MascotSlot`'s `thinking` expression, off under `prefers-reduced-motion`) — for 10 the loop — inline in app/screens/LoopScreen.module.css. `MascotSlot` only renders a static expression; no Storybook component covers motion. The bob's ±4px offset is a motion detail, not a spacing/sizing value, so it isn't read from `tokens/tokens.json`.
- Voice waveform row (bars right-anchored and building from the right edge toward the left, one at a time, while there's sound in Recording; held frozen and pulsed like a slow equalizer in Processing) — for 10 the loop — inline in app/screens/LoopScreen.tsx / LoopScreen.module.css (`VoiceWaveform`, 2026-09-16, your call; reveal mechanism reworked twice same day, first per `plans/004-continuous-right-entry-waveform-reveal.md`, then reversed again — the plan's own left-anchored/append-rightward growth read as the opposite of "comes from the right side of the screen" once seen live, so the row's `justify-content` flipped to `flex-end` and the reveal now slices `WAVEFORM_BAR_HEIGHTS` from the end backward instead of from the start — replaces the original word-count-driven vertical grow-in). No Storybook component covers this or any mascot/decoration motion, same root cause as the Processing bob above and the exam-plan hint arrow below. Bar width (`space.150`) and gap (`space.100`) are real tokens — they happen to sum to Figma's own 10px pitch exactly — the row's 28px inset (`space.400` + `space.300`, matching Figma node `13696:7029`) and the entrance's 24px slide distance (`space.600`) are likewise real tokens — but the 38 individual bar heights are literal pixel values transcribed off Figma nodes 13698:7196 / 13696:7029 (08Talking-finished / Processing draw the identical row), the same frame-geometry exemption the hint arrow and the summary stat chip already use. The reveal interval (200ms), entrance duration/curve (220ms, `--ease-out-expo`), and the pulse animation's duration and stagger are motion values with no token, same gap as the Processing bob.
- 02Hint-animate's hint (Option B, the purple line: a live SVG arrow composited over the still frame export, its curve a real transcription of Figma's own vector geometry, self-drawing via `stroke-dashoffset`, ending in a hand-built arrowhead at the Voice recall chip) — for 3 exam plan — inline in app/screens/ExamPlanScreen.tsx / ExamPlanScreen.module.css. No Storybook component covers this (or any) mascot/decoration motion, same root cause as the Processing bob above. Frame geometry (the arrow's position/rotation matrix) is Figma's own, not a design value, same exemption as frames.ts's tap-zone boxes; the cubic-beziers and animation timings are motion values with no token, same gap as the Processing bob. Supersedes this line's earlier Option A build (a live `MascotSlot` bounce + chip-pop), reconsidered and replaced the same day (sprint-context.md, 2026-09-16) — not two gaps, one gap whose fill changed.
- Explanation paragraph with only the judge's missed concepts bolded (plain `<strong>` spans, tokens only — no rich-text component in Storybook) — for 7 Why? sheet — inline in app/screens/WhyScreen.tsx, content composed by the caller (`PrototypeFlow.tsx` and the screen's own stories), not the screen itself.
- Bare grab handle, replacing `BottomSheetAppBar`'s own `Default` variant — for 7 Why? sheet — inline in app/screens/WhyScreen.tsx. `Default` renders nothing but the handle when no title is set, but still reserves the full space.1600 (64px) zone its titled variants need; this screen has no title at all, so a plain handle (same token values as `BottomSheetAppBar`'s own `.handle`) sits in a space.400 (16px) zone sized for a handle alone instead. Not a Storybook gap so much as a shared component's only no-title variant not fitting a screen with genuinely no title.
- Latency switch: a 3-way segmented control (Normal / Slow / Hang) for the facilitator's forced-latency override — for 2 Turn log — inline in app/screens/LogScreen.tsx. No toggle or segmented-control component exists in Storybook or design-system.md. Decided in sprint-context.md (2026-09-14) but not named as a component in `SPEC.md`'s screen 2 text. Second occurrence: the "Next answer" 6-way version of the same control, picking which of 10 the loop's sample answers Recording plays next since real STT is deferred — same file, same reasoning.
- Turn log rows: session dividers and turn cards, tokens only, no data-table or list component in Storybook — for 2 Turn log — inline in app/screens/LogScreen.tsx. `SPEC.md` still says "the rows use tokens only" for this screen.
- Stat chip (a coloured card — a small label on its own fill, above a dark inset box holding an icon and value in that same colour) — for 8 Summary sheet — inline in app/screens/SummaryScreen.tsx. Tokens read off the real Figma frame (node 7366:69693); no matching component in Storybook or design-system.md. Replaces this screen's earlier per-question row list (built 2026-09-15, replaced the same day to match reference/Finish-quiz.png — your instruction).
- 00Homescreen's exam-tab "new" badge (design-system.md's `badge`), animated — a live dot plus a looping ping ring composited over the still frame export in place of the badge's own baked-in pixels — for 3 exam plan — inline in app/screens/ExamPlanScreen.tsx / ExamPlanScreen.module.css. Same root cause as 02Hint-animate's arrow above: no Storybook component covers mascot/decoration motion. Colors are the same tokens the real instance already binds (`accent/coral/bold` fill, `accent/coral/on-bold` stroke), already documented in design-system.md's own `badge` entry, so no new color decision was needed. Position is the one departure from the live file in this whole build: pinned to the exam-tab icon's own top-right corner rather than the real badge instance's own (inset) coordinates, at your direction (sprint-context.md, 2026-09-16) — every other geometry value in this build is a straight transcription, this one isn't. The ping's duration/easing/scale are motion values with no token, same gap as the Processing bob and the arrow.
