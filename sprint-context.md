# Sprint Context

## What this is
Voice-based active recall for Knowunity. Student speaks a term aloud, Knowie judges pass/partial/fail in text only.

## Committed concept
Voice recall is a toggleable mode on the exam plan, not a new node type. Turning it on changes how existing topic nodes work.

## Where the recall step lives
Exam plan path screen. A "Voice recall" toggle pill sits at top; when on, topic nodes swap to a mic icon and open into the voice loop instead of their normal activity.

## Decisions log
- Toggle off, nodes keep current default behavior unchanged, this sprint designs only the on-mode variant, toggle, and banner
- Node icon swaps to mic only when mode is on, because the icon signals mode state, not step type
- Toggle label is "Voice recall" with no on/off suffix, state is shown by the pill's fill, not by text
- A readiness banner ("Voice recall is ready... Show me") appears above the section before a node is reached, because prior steps give no warning that speaking is coming
- "Show me" highlights the toggle, it does not jump into the loop, because enabling the mode comes first
- Push to talk only, no auto-endpointing, because guessing when someone stops talking is voice's most common failure
- Knowie never speaks, every response is text
- Verdict is pass, partial, or fail, not binary, because a false "wrong" costs more here than in multiple choice
- Transcript is always shown back, so a wrong verdict reads as heard wrong, not app broken
- Text fallback reachable in one tap every turn, because voice-only excludes students who can't speak right now
- Skip is available on any term, no hard wall
- transcriptDisplay's four states (Empty, Filled, Overflow, Silence) each have fixed content, not one shared property
- buttonVoice's Recording state is a CTA-text-only swap, no mic/waveform icon exists yet to animate
- bottomSheetVerdict's Partial state uses accent/blue as a placeholder, no real Partial token exists yet
- Button component (app/components/Button.tsx) built from the real Figma `button` component set. Its inner-shadow bezel (black @15%, −2px/−4px offset) had no matching token, so `color.alpha.dark-15` and `size.depth.Negative 050` were added to tokens.json to cover it rather than approximate. Letter-spacing (1%/0% by size) is still skipped: tokens.json has no letter-spacing group, and CSS doesn't support `%` for it anyway.
- Button's `state` variant (Pressed) is not exposed as a prop; Pressed is CSS `:active`, Disabled/Loading are real `disabled`/`loading` props, so real interaction and accessibility semantics stay correct
- Secondary button built borderless: Figma sets strokeWeight=3 but its `strokes` array is empty, so there's no real border to reproduce
- TextBlock component (app/components/TextBlock.tsx) built from the real Figma `textBlock` component set (variant XL/L/M/S, showCaption toggle, title/caption text props). Each variant's title/caption bind fixed named text styles read off the real component (XL: Display M / Headline XS Regular, L: Headline XL / Headline XS Regular, M: Body M Bold / Caption M Regular, S: Body S Bold / Caption S Regular), title binds `text/primary`, caption binds `text/secondary`. Letter-spacing skipped, same reasoning as Button: no letter-spacing group in tokens.json. Frame hugs content (no fixed width), matching Figma's own auto-sizing.
- TextBlock's and MascotSlot's stories needed a `background/page` decorator: neither component paints its own fill, so the isolated Storybook canvas defaults to white and the a11y color-contrast check flags text/primary and text/secondary against it, even though that pairing is correct on this project's real dark screens. No precedent for story decorators before this; added one rather than leaving the false-positive as noise.
- MascotSlot component (app/components/MascotSlot.tsx) built from the real Figma `mascotSlot` component set. Only real exposed prop is `size` (XL/2XL/3XL/4XL) — confirmed by instantiating the real component and reading back its `componentProperties`. Box sizes match `size.illustration.{800,1500,2500,4000}` exactly; padding is a constant `size.space.300` (12px) at every size, not scaled.
- MascotSlot's `expression` prop is a deliberate exception to "only expose what Figma exposes": the mascot expression is an internal "Homie" instance-swap, not exposed to mascotSlot's own instances in Figma — but a sweep of all 28 real instances in the file found designers already overriding it by hand (standby, thinking, excited, approving all in active real use, including the "approving" celebration moment). Since the artwork already exists as local assets (public/images/*.svg) and the real file already needs this, `expression` was added ahead of Figma's own interface rather than left broken. Still an open item to raise with Harry: expose Homie formally in Figma and rebuild to match: this prop should be treated as ahead of the source of truth, not a mirror of it.
- BottomSheetVerdict component (app/components/BottomSheetVerdict.tsx) built from the real Figma `bottomSheetVerdict` component set (variant: Success/Partial/Error/Silence). Live inspection found the component's own description is wrong in several places: (1) "every variant's action buttons are real component instances, not hand-drawn" is only true for Silence — Error/Partial/Success each hand-draw a pill frame mimicking `button` without being wired to it, using different colors than the real `button` component (`interactive/secondary`+`onSecondary` vs. the real component's `background/surface`+`text/primary`), and Error's hand-drawn text uses "Greed VF-TRIAL" Condensed Heavy at 24px, a font/size combo with no matching token anywhere in tokens.json. (2) Background tinting is more asymmetric than documented: Success tints (`feedback/success/subtle`) and Partial also tints (`accent/blue/subtle`, not called out in the description) — only Error/Silence stay neutral (`background/surface`).
- Per your explicit decision, BottomSheetVerdict's CTA row was rebuilt using real `button`/`buttonGroup` instances in all four variants (not just Silence), reattaching the real components instead of reproducing the stale hand-drawn state — matches design-system.md's own rule against documenting a stale duplicate as valid. The action button's per-variant color (feedback/error/success/bold or accent/blue/bold) is a local style override on `button`'s real Primary look, the same pattern already established for `buttonVoice`, since none of `button`'s own real variants carry these colors.
- Two prerequisite components didn't exist yet and were built to support BottomSheetVerdict: ButtonGroup (app/components/ButtonGroup.tsx, Figma's real `buttonGroup` set, variant Horizontal/Vertical × size M/L, exactly two children) and ButtonVoice (app/components/ButtonVoice.tsx, Figma's real `buttonVoice` set, wraps `button` Secondary/L per design-system.md's own description). ButtonVoice's `ctaText` prop is a MascotSlot-`expression`-style exception: not a real exposed Figma property, but the real Silence instance already overrides the default state's text to "Re-record" per-instance, so it was added ahead of Figma's own interface.
- BottomSheetVerdict's header "Descriptor" subtitle text layer is dropped — confirmed always the unused placeholder "Sub-title (optional)" in all 4 component definitions and all 3 real placed instances, and never mentioned in design-system.md's own description. This is a *different* layer from Silence's own separate supporting text ("No worries, this one doesn't count against you."), which is real content and was kept as fixed per-variant copy (same convention as `transcriptDisplay`'s fixed per-state content).
- BottomSheetVerdict's drag handle is real `layoutPositioning: ABSOLUTE`, centered independent of layer order — the file orders it inconsistently across variants (before vs. after the top nav row), but since it's absolutely positioned this has no effect on the rendered result. Its 2px corner radius is bound to a variable literally named "Stroke/Heavy Border" (`size.stroke.heavy-border`) rather than a radius token — reproduced as-is since that's what's actually bound, not substituted with a same-numbered radius token.
- Thumbs up/down feedback control has no active/selected state anywhere in the real file — built as plain icon buttons with callback props, no toggled visual state. Flagged as an open gap, same treatment as `badge`'s missing VoiceOver label.
- TranscriptDisplay component (app/components/TranscriptDisplay.tsx) built from the real Figma `transcriptDisplay` component set (state: Empty/Filled/Overflow/Silence). `transcript` is a real exposed prop for Filled/Overflow only — the description's "fixed content, don't expose a shared property" warning is read as "don't collapse every state into one shared value" (the real bug it describes), not "can never vary": this component's whole purpose is showing the student's actual spoken words, so hardcoding Figma's demo copy would make it unusable. Empty/Silence stay genuinely fixed system copy, matching the description's own intent. Same ahead-of-Figma pattern as MascotSlot's `expression` and ButtonVoice's `ctaText`.
- TranscriptDisplay's Overflow state is built at a fixed 320px height (`size.illustration.4000`, reused across categories same as `buttonIcon`'s circle diameter), not the 448px the component's own description claims. Live inspection found the real component and 5 of 6 real placed instances are fixed at exactly 320px; the one exception fills its parent's height contextually (510px in that screen). 448px doesn't match any real bound value found anywhere in the file — looks like a stale note in the description.
- TranscriptDisplay's width is fluid (100%), not a fixed 358px — 358px isn't bound to any Figma variable, but is consistently `390 − 2×16px` (mobile width minus two `size.space.400` margins) across every real instance, so the component fills its container instead of hardcoding a pixel width that only works at exactly 390px.
- 2026-09-11: Overflow updated in Figma to add 96px padding (`size.space.2400`, a real bound variable) above and below the clip window, growing the component's own total height to 512px (96+320+96) where it was previously flush at 320px. The 320px clip window itself is unchanged. Rebuilt as an outer padded wrapper (`.overflowWrapper`) around the clipped text rather than folding padding into the clip window, so the visible window stays exactly 320px. 448px (the description's claimed box height) still doesn't match anything real before or after this update.
- AiDisclaimer component (app/components/AiDisclaimer.tsx) built from the real Figma `aiDisclaimer` component (no variants, zero exposed properties, matches design-system.md exactly). The real bound copy doesn't match the description's quote ("AI-generated content may be incorrect. Check it for accuracy."); the actual text is the shorter "Knowie is AI and can make mistakes." Built using the real bound text per this project's established precedent (live file is source of truth). Padding (`size.space.100` top, `size.space.800` bottom) is real and bound on the component itself, confirmed consistent across all 11 instances.
- 2026-09-11: aiDisclaimer's typo ("Konwie" → "Knowie") fixed directly in Figma at your request, not just in code — edited the master component's text node (font loaded, characters set), confirmed the fix propagated to all 11 real placed instances (no exposed text property on this component, so instances have no way to diverge from the master). AiDisclaimer.tsx updated to match.
- 2026-09-11: transcriptDisplay's Empty and Silence states recolored in Figma from `text/secondary` to `text/tertiary` (a real bound variable change, confirmed on both component definitions), dimming them further. TranscriptDisplay.module.css updated to match. Worth noting: `aiDisclaimer`'s own description explicitly rules out `text/tertiary` for "anything a student must read," and Empty/Silence are both instructional copy the student needs to read — flagged this tension in the component's Storybook docs in case it wasn't the intended target of the change, but built to match the real file as instructed.
- 2026-09-11: ProgressIndicator component (app/components/ProgressIndicator.tsx) built from the real Figma `progressIndicator` component set (variant Primary/Coral × thickness 24/16 × progress 0/25/50/75/100, showText toggle). Checked Storybook first — nothing existing covers progress display, so this is new. Fill width implemented as CSS percentages per the five snap points rather than transcribing each variant's specific (non-proportional, hand-placed) pixel width. Track width built fluid (100%) rather than Figma's fixed 350px frame — unlike `transcriptDisplay`'s 358px, nothing confirms 350px as a real repeated dimension, and real usage is inside appBar's flexible Slot. Track/fill corner radius substitutes `radius/Full` for Figma's raw unbound `12` (not in tokens.json's radius scale, but 12px and `radius/Full` render pixel-identical on a 16–24px bar, and `radius/Full` is already the bound token on this component's own fill rect). Track thickness (16/24) reuses `size.icon.200`/`size.icon.300` in place of a raw pixel number, same token-borrowing precedent as Chip's fixed control heights. Real token gap found: Container's invisible stroke binds to a `border/subtle` variable in Figma that doesn't exist in tokens.json's `border` group — not implemented since the stroke is invisible in every real variant anyway.
- ProgressIndicator's `showText` toggle reveals a real hidden "Unit Progress" text layer whose fixed content is a step-count fraction ("0/12" as sampled from the file), not a percentage, and isn't wired to the `progress` value in Figma (identical across all five steps). Reproduced as the same static placeholder rather than invented as a computed counter — flagged as an open gap, same category as `buttonVoice`'s unwired icon swap. Default `aria-label` ("Lesson progress") added to the root `progressbar` role to fix an a11y violation (no accessible name) surfaced by story tests; overridable via the existing `aria-label` prop.
- 2026-09-11: Live comparison against the real Figma component set found two build bugs, fixed: (1) the "Unit Progress" label is absolutely positioned centered *over* the track in Figma (not beside it with a gap — the root frame's `itemSpacing` is vestigial, since `layoutMode` is `NONE`), and the label layer only exists at all on `thickness=24` variants (`thickness=16` has none) — `showText` is now a no-op at `thickness=16` and the label renders as an absolute overlay, matching the source. (2) The `progress=0` "nub" at `thickness=24` was rendering as a 24×20px stadium instead of Figma's 20×20px circle, because its `min-width` used the raw thickness token (`size.icon.300`) instead of the inset-adjusted content height — fixed to use `size.icon.250` (20px), which matches the track's height after its 2px inset on each side.
- 2026-09-11: BottomSheet (app/components/BottomSheet.tsx) and BottomSheetAppBar (app/components/BottomSheetAppBar.tsx) built from the real Figma `bottomSheet` (3675:30952) and `Bottom-sheet App Bar` (5101:7358) component sets. Neither set, nor any of their 3 + 4 variants, carried any Figma description, and neither appears in design-system.md at all. Per your decision, descriptions were drafted during this build and written back into both component sets' Description fields in Figma (following design-system.md's own rule), with the same text used as the Storybook docs.
- BottomSheet's `height` (S/M/L) is the sprint's one agreed non-token value. All three Figma variants are `layoutSizingVertical: HUG`, so their 300/494/768 totals aren't enforced sizes — they're whatever the two empty slots happened to measure, and they're mutually inconsistent (S: 114/114, M: 364/58, L: 348/348), with no matching token for any of the three. Your call was to implement them as viewport detents (`max-height` 40/60/90dvh): a cap rather than a fixed height, so short content still hugs. Consequence: `middleSection` now scrolls (`overflow-y: auto`, `justify-content: safe center`) once the cap bites — the same "no scroll behaviour designed" gap `transcriptDisplay`'s Overflow state already flagged.
- BottomSheetAppBar reuses the existing ButtonIcon (variant=Tertiary, size=M) for its icon buttons rather than building a new one: Figma's separate `App Bar Button Icon` set resolves to exactly what that already renders (48px tap target, transparent 40px `radius/Full` circle, no bezel nudge), and its `state` axis (Default/Pressed/Disabled/Loading) maps 1:1 onto ButtonIcon's existing Pressed-as-`:active` / disabled / loading model. Only deviation: Figma's iconSlot frame is 24px around a 20px glyph, ButtonIcon's M icon box is 20px — the painted glyph is 20px either way.
- The app bar's icons are not real: all four Types point at iconSlot's unassigned `square` placeholder, so `dismissIcon`/`actionIcon` are props and omitting them renders that same square rather than inventing an X. Two further divergences from the already-built `bottomSheetVerdict` sheet, both worth reconciling at the source: this set binds its handle fill to `background/floating` where bottomSheetVerdict uses `background/stacking`, and this set draws no top border where bottomSheetVerdict draws a `border/default` hairline.
- Open item raised by this build, not resolved: `bottomSheet` has exactly one instance in the whole file, on a "Working log" frame, resized to 400×444, matching no variant — no confirmed real usage to build against, same situation `snackbar` is in.
- 2026-09-11: `Bottom-sheet App Bar`'s variant axis renamed from `Type` to `variant` **in Figma** (via `editComponentProperty` on the set), bringing it in line with design-system.md's rule that the three standard axes are `variant`/`size`/`state`. All four variants now read `variant=…` and every nested instance followed automatically, including the one placed sheet. React prop renamed to match. Still outstanding at the source: the component itself is named in Title Case (`Bottom-sheet App Bar`) where the convention is camelCase (`bottomSheetAppBar`), and its variant values mix casing (`Default` vs `withTitle`).
- 2026-09-11: design-system.md gained entries for `bottomSheet` and `Bottom-sheet App Bar`, which had none. Both were also missing Figma descriptions entirely; those were written during the build and pushed to the component sets' Description fields.
- Two corrections to the earlier BottomSheet build notes, found while verifying the rename propagated: (1) the embedded app bar is NOT uniformly Default — height M and L embed variant=Default, but height S embeds variant=dismissAndAction. Nothing about a sheet's height should change whether it has a close button, so the React build uses Default for all three and treats S as an authoring slip. (2) The one placed instance overrides its app bar to dismissAndAction purely to carry the text "Let's avoid titles & buttons up top" / "Let's avoid captions whenever possible" — designer guidance written into the content rather than real content, now reflected in both the Figma description and the Storybook docs.
- The `bottomSheetVerdict` handle/border divergence was confirmed real in Figma, not an error in the earlier React build: bottomSheetVerdict bound its handle fill to `background/stacking` and drew a 1px top-only `border/default` hairline (strokeTopWeight 1, strokeBottomWeight 0, INSIDE), while `bottomSheet` bound its handle to `background/floating` and drew no stroke at all. Both handles were otherwise identical (32×4, centred, 6px from the top, ABSOLUTE), except bottomSheetVerdict bound its 2px radius to `Stroke/Heavy Border` where bottomSheet left the same 2 unbound. Note `border/default` and `background/stacking` both alias `alpha.light-10`, so on bottomSheetVerdict the handle and the hairline render the identical colour.
- 2026-09-11: The bottomSheet/bottomSheetVerdict sheet-chrome divergence was resolved **in Figma**, at your direction, rather than reproduced in code. `Bottom-sheet App Bar`'s handle fill moved from `background/floating` to `background/stacking` across all four variants (the old binding composited to roughly `#323337` on `background/surface` — nearly invisible; the new one to roughly `#393a44`), and its 2px corner radius bound to `Stroke/Heavy Border` where it had been an unbound raw `2`. `bottomSheet` gained the same 1px top-only `border/default` hairline bottomSheetVerdict already had (strokeTopWeight bound to `Stroke/Border`, other edges 0, INSIDE). Verified by screenshot at 4x. React CSS updated to match. Since `border/default` and `background/stacking` both alias `alpha.light-10`, the hairline and handle intentionally render the same colour. Still unreconciled between the two sheets: bottomSheetVerdict is 390 wide, bottomSheet 350.
- Also noticed while verifying: the dismiss button in bottomSheet's height=S variant does carry a real X glyph as an instance override, so a close icon exists somewhere in the library even though `App Bar Button Icon`'s own default is still the unassigned `square` placeholder.
- 2026-09-11: BottomSheet keeps `role="dialog"` but no longer asserts `aria-modal`, and `aria-label` is now a required prop. `aria-modal` is a promise that content outside the sheet is inert: it confines a screen reader's cursor but does nothing to keyboard focus, so asserting it without a real focus trap leaves keyboard and screen-reader users seeing different things. The modality lives in `scaffold` (its Bottom-sheet background and `bottomSheetOnly` slot), not in this component, so the consumer owns focus trapping, inertness, `aria-modal` and Esc-to-close. Requiring `aria-label` follows `buttonIcon`'s existing precedent and makes an unnamed dialog impossible to ship. Still inconsistent: `bottomSheetVerdict` renders a plain `<div>` with no role at all — the two sheets should eventually agree.
- 2026-09-11: IconSlot component (app/components/IconSlot.tsx) built from the real Figma `iconSlot` component set (`Size (IGNORE)`: 100/150/200/250/300/400, plus an instance-swap for the icon). All six sizes bind cleanly to `Size/Icon/*` in Figma and already exist as `--size-icon-*` (8/12/16/20/24/32px), so there were no token gaps. The slot paints nothing — no fill, padding, radius or colour — and the icon inside is expected to use `currentColor`, the same contract `button` and `buttonIcon` already document. Default kept at Figma's own `400` even though real usage varies across 200/250/300, the same convention `chips` follows.
- The axis is deliberately NOT renamed in Figma, unlike `Bottom-sheet App Bar`'s `Type` → `variant`. That one was a pure naming-convention fix; `Size (IGNORE)` instead encodes an open question the component's own description raises ("either the label is stale or 99 instances are already doing it wrong; confirm with Harry"). Renaming it now would quietly erase the flag that prompts that conversation. The React prop is `size` because the literal isn't a valid JS identifier — the six values are unchanged.
- Two authoring inconsistencies in the real `iconSlot` set, built as the consistent version rather than copied: only the `100` variant binds both width and height (the other five bind width only, leaving height raw), and `250` is `layoutSizingHorizontal: FILL` where the other five are FIXED, so it would stretch rather than stay 20px square. All six render as fixed squares today, which is what the React build does.
- Not done, worth considering: `button`, `buttonIcon`, `chips`, `snackbar` and `bottomSheetVerdict` each hand-roll their own icon box from the same `size.icon.*` scale (~16 declarations across five stylesheets). IconSlot is what they would collapse onto, but that refactor was left out of this build. `button`, `buttonIcon` and `chips` are exact drop-ins — their base `.icon` rule is already identical to IconSlot's `.slot` — and `snackbar` is near-identical but would gain centring it currently omits.
- BottomSheetVerdict is the exception, and checking Figma explains why: only its Partial variant uses a real `iconSlot` (an instance at 24x24 with the glyph filling it). Error/Success/Silence have no icon container at all, they're raw vectors, exactly as design-system.md says. So the React build's 32px `.iconBox` has no Figma counterpart — it's a layout box invented here to give those three a consistent footprint, and IconSlot should not replace it. When that component is next open: migrate Partial only (to `<IconSlot size="300">`, which deletes both of its special-case CSS overrides) and rename `.iconBox` to something that doesn't read as an icon slot. Deliberately not adding a fill/no-fill escape hatch to IconSlot: a smaller glyph inside a larger box is an IconSlot nested in a layout box, i.e. composition, not a prop.
- 2026-09-11: Dropped two no-op CSS declarations from BottomSheet (`gap` and `padding-top`, both bound to `Space/0` in Figma and both already the browser default), replacing them with a comment recording the binding. Transcribing zero-valued bindings as live CSS is dead code that invites a reader to hunt for an effect that isn't there, and it was already inconsistent with `BottomSheetAppBar`, where the vestigial `itemSpacing: 4` was omitted for the same reason. The rule is "never invent a value that isn't in tokens.json", not "transcribe every binding".

- 2026-09-11: Scaffold component (app/components/Scaffold.tsx) built from the real Figma `scaffold` component set (3085:9242). The node handed over (`4589-35266`) turned out to be a stale duplicate: an instance named `scaffold` whose main component is `Screen/S - Pixel 2`, a 411x731 Android frame with no variant axis, orphaned off-canvas (`parent === null`) with exactly one instance in the file. The live set is what all 15 example screens use and what design-system.md already describes (slots `topNavigation`/`middleContent`/`bottomContent`/`bottomSheetOnly`, booleans `showTopNavSlot`/`showBottomNavSlot`/`showBottomSheetBackground`, `size` axis of 8). Built from the live set; the stale copy should be deleted at the source, per design-system.md's own never-list. Named `Scaffold` rather than `Screen` to match the live Figma set and every other component's Figma-name-to-PascalCase mapping in this repo.
- `size` ships with one option (`iPhone 13`) by your decision, not all 8 — CLAUDE.md is 390px iOS only and design-system.md rules out the tablet/desktop variants. The prop drives nothing visually: Figma's 390x844 frame is unbound to any token, so the frame is fluid (`width: 100%`, `height: 100dvh`), the same call BottomSheet, TranscriptDisplay and ProgressIndicator each already made for their own unbound frame dimensions.
- Status bar renders the time only, by your decision. Figma's Status Bar is an iOS device-kit instance (`Mode=Night`) whose colors are bound (`text/primary`, `background/inverse`) and whose 48px height is exactly `space/1200`, but whose wifi/cellular/battery glyphs (15x11, 16x11, 24x11 on absolute offsets) and -0.3px time tracking have no tokens behind them. Glyphs dropped rather than hardcoded; the time's 15px matches `--font-size-sm` exactly via Body S Regular, and its 23px offset resolves to `space/600` (24px), the nearest real token. `aria-hidden` — a mock clock isn't content.
- Real token gap, and this build's one color substitution: the Panel Header's 1px bottom hairline binds `Core/Grayscale/Dividers`, a remote primitive from another library at rgba(255,255,255,0.15) that isn't in tokens.json. By your decision it renders as `border/default` (rgba(255,255,255,0.102)) rather than adding a competing divider token — that's the semantic token for the role, and design-system.md forbids consuming a primitive directly anyway.
- `showScrim` is added ahead of Figma's interface (same pattern as MascotSlot's `expression` and ButtonVoice's `ctaText`), because the `Scrim` layer inside `middleContent` is wired to no component property and so can't be toggled from an instance at all. Also a correction to design-system.md, which described it as "dimming this content when a bottom sheet is showing over it": the layer is actually a bottom-anchored gradient from transparent to `background/page`, i.e. a content fade-out. The modal dim is the separate `Bottom-sheet background` rectangle, which is the one bound to `background/scrim`. Its 151px height has no token; `--size-space-4000` (160px) is used as the nearest real one, a 9px substitution rather than an invented value.
- Figma wires `bottomSheetOnly`'s visibility to `showBottomNavSlot` — the same boolean as the bottom nav, on both the live set and the stale copy. Deliberately not reproduced: the bottom nav is normally hidden while a sheet is up, so honouring that link would hide the sheet exactly when it's needed. The React slot renders on having content instead. Needs its own boolean or none at the source.
- The root's `itemSpacing: 10` is vestigial and not transcribed: the frame is SPACE_BETWEEN with a FILL child (`middleContent`), so Figma ignores itemSpacing and the four sections stack flush (48 + 60 + 702 + 34 = 844 exactly). Declaring it as a `gap` would push the layout 30px past the frame, and 10 isn't a token either way. Same reasoning as the `Space/0` bindings dropped from BottomSheet.
- `middleContent` clips rather than scrolls, matching Figma's `clipsContent` slot — the same unresolved question transcriptDisplay's Overflow state already flags. Judged safe here because a screen's actions live in `bottomContent`, outside the clipped box. The four slots render as plain divs with no landmark elements: Figma defines no semantics, and a Storybook docs page renders every story at once, where multiple `<main>` elements would itself be an a11y violation.

- 2026-09-11: Renamed the React component from `Scaffold` to `Screen` at your request. This is the one place the repo's Figma-name-to-PascalCase mapping doesn't hold — the live Figma set is still named `scaffold` — and it collides with the stale `Screen/S - Pixel 2` copy, so "Screen" means two different things depending on whether you're in Figma or in this codebase until that stale component is deleted.
- 2026-09-11: Line-by-line diff of the React build against `scaffold / size=iPhone 13` (4794:5828) found 20 differences; 17 were deliberate translation choices already logged above, 3 were real bugs, all now fixed.
- Bug 1, Panel Header was 1px too tall. Figma's stroke is INSIDE, so the header measures 48 in total with the hairline drawn over the status bar's last pixel row; a CSS `border-bottom` added its 1px outside that, making the header 49 and pushing every screen down a pixel. Now drawn as `box-shadow: inset 0 calc(-1 * var(--size-stroke-border)) 0 var(--color-border-default)`, which paints inside without affecting layout — the accurate translation of an INSIDE stroke, and still token-derived.
- Bug 2, empty slots painted strips. Every Figma slot carries `displayEmptyByDefault: false`, so it renders nothing when nothing is inserted, even with its boolean on. `topNavigation` and `bottomContent` now render only when they have children — an empty `bottomContent` was painting a bare 32px strip of its own `space/400` padding, the same empty-section case BottomSheet already omits. `middleContent` collapses too, with one exception: it still renders when `showScrim` is on, so the fade stays reachable on an otherwise empty screen.
- Bug 3, the status bar time sat ~7px left of Figma's. Figma sets it in a fixed 54px box at x=23 with the text centred inside, so the glyph centre lands at x=50 regardless of numeral width; the build had left-aligned text against a padding value, which also made the position depend on Greed's own metrics. Rebuilt as a fixed centred box matching Figma's structure, using the nearest real tokens — 23 → `space/600` (24) on the bar's inline padding, 54 → `illustration/700` (56) on the box — putting the centre at 52 against Figma's 50, down from ~43, and no longer drifting with the font. Borrowing `illustration/700` for a text box is the same cross-category token reuse ProgressIndicator's thickness and Chip's control heights already use; flag it if you'd rather treat 54 as a missing token instead.
- Worth noting from the same diff, not changed: `Bottom-sheet background` is 440x956 at (-25,-112) in Figma (it overhangs the frame and is then clipped) where the build uses `inset: 0` — the painted area is identical, the geometry isn't. The Status Bar instance also carries a real `Mode=Night` variant that isn't modelled as a prop, since only the Night mode is in scope for a dark-mode-only prototype.

- 2026-09-11: Storybook's a11y test mode moved from `todo` to `error` in `.storybook/preview.tsx`, after verifying the whole suite is clean at that setting (73 story tests, 20 components, zero axe violations). Violations introduced from here on fail the run instead of being a warning nobody reads. Verified the checker actually runs with a throwaway probe story carrying a bare `<button>` and an `<img>` with no alt: both were caught (`button-name`, `image-alt`, critical), then the probe was deleted.
- Known limitation of that setting, and the reason the contrast audit below was done by hand: axe returns `incomplete` ("needs review"), not a violation, when the foreground colour has alpha. `text/secondary`, `text/tertiary` and `text/disabled` are all alpha tokens, so a green a11y run is NOT evidence that this system's text contrast passes.
- 2026-09-11: Contrast audit computed directly from `build/css/tokens.css` with real alpha compositing (57 pairings: every `text/*` token over the three screen surfaces, every semantic `on-*` contract pair, and text over the tinted verdict panels), plus the nested-backdrop cases the flat matrix misses. Six pairings land below 4.5:1, none below 3:1. Nothing fixed yet.
- Contrast finding 1 (lowest in the system): `text/disabled` on `interactive/disabled` reads 3.34:1 over `background/surface` and 3.75:1 over `background/page` — the disabled label on `button` and `buttonIcon`. Worth knowing before changing anything: WCAG 1.4.3 explicitly exempts inactive UI components from the contrast minimum, so this is technically compliant. It is still the least readable text the system produces.
- Contrast finding 2: `text/tertiary` on `background/surface` reads 4.34:1, just under the 4.5:1 that `transcriptDisplay`'s 18px regular Empty/Silence copy needs. It passes today at 4.62:1 because the component sits in `scaffold`'s middleContent over `background/page` — it would fail the moment that transcript is placed on a sheet. This is the concrete number behind the tension already logged above, where Empty/Silence were recolored to `text/tertiary` while `aiDisclaimer`'s own description rules that token out for anything a student must read.
- Contrast finding 3: `accent/brand/on-subtle` on `accent/brand/subtle` reads 4.10:1. No component uses that pair yet. Note the `on-bold` tokens' descriptions explicitly promise ">= 4.5:1" while the `on-subtle` ones carry no such promise — worth deciding whether that omission is intentional.
- Not a finding, recorded so it isn't re-raised: `text/link` on `background/surface` reads 4.43:1, but its only use is the Tertiary button's spinner tint, which is a graphic judged at 3:1 (WCAG 1.4.11), not text. It passes. Everything else clears comfortably — `text/primary` 13.95-17.63:1, `text/secondary` 7.23-8.36:1.
- Operational note: editing `.storybook/preview.tsx` wedges the addon-vitest worker (idle, 0% CPU, every `test-run` times out). Killing the worker alone doesn't recover it, since the addon only spawns it with the dev server; a full `npm run storybook` restart does. Hit twice this session.

- 2026-09-11: Storybook published to Chromatic (build 1 passed: 20 components, 73 stories, 73 snapshots). Library URL: https://6aa4bc244276566b3a47a0b6-cvluvgfdix.chromatic.com/ — project setup continues at https://www.chromatic.com/setup?appId=6aa4bc244276566b3a47a0b6. `chromatic` added as a devDependency and `npm run chromatic` added to package.json.
- The project token is deliberately NOT in any committed file. It lives in `.env.local`, which `.gitignore` already covers via `.env*` (verified with `git check-ignore`). The npm script is `node --env-file-if-exists=.env.local node_modules/chromatic/dist/bin.cjs` rather than a bare `chromatic` call: Chromatic's bundled dotenv would only read `.env`, not Next's `.env.local`, and `--env-file-if-exists` (Node 22.9+; this machine is on 24.20) means the script still works in CI where the token comes from a real secret instead of a file. Consequence worth knowing: a fresh clone has no `.env.local`, so whoever runs it next needs the token from you or a `CHROMATIC_PROJECT_TOKEN` already in their environment.
- Chromatic reported "Commit '3ec4f73' on branch 'main'; no ancestor found" — expected on a first build, but note it built the working tree, so the Screen component and the a11y config change were included in the snapshots despite being uncommitted.

- 2026-09-11: Colour diff of BottomSheetVerdict against the real Figma set (13562:5922), verified by measuring computed styles in a real browser (Playwright) rather than reading CSS. Eleven colour differences found, all now matched to Figma and re-measured to confirm.
- The big one was a genuine bug: all four header icons rendered **pure black**. The SVGs paint with `fill="currentColor"`, but neither `.iconBox` nor `.icon` set a `color`, and Storybook loads only `build/css/tokens.css` and `preview.css` — never `app/globals.css` — so `currentColor` fell through to the browser default. Fixed by binding `.iconBox`'s colour per variant to the same token the title already uses (`feedback/error/bold`, `accent/blue/bold`, `feedback/success/bold`), which is what Figma binds on the icon vectors. Worth noting `buttonIcon`'s own prop docs already state the contract this violated: "Should paint with currentColor so it picks up the right tint per variant/state automatically."
- Silence's supporting text was `text/primary`; Figma binds `text/secondary`. Matched.
- The sheet's Secondary actions ("Why?" on three variants, the Skip icon button on Silence) now carry a local `interactive/secondary` + `interactive/onSecondary` override instead of inheriting `button`'s own Secondary. Confirmed directly on the master that `button`'s Secondary genuinely is `background/surface` + `text/primary` — so the earlier build was faithful to the master, but on the Error and Silence sheets (both `background/surface`) that made the button invisible against its own sheet. Same local-override mechanism ACTION_COLOR already used. Open system-level question for Harry: `button` Secondary is unusable on any `background/surface` surface, which is every sheet in the system.
- CORRECTION to an earlier claim in this session: only Silence's "Re-record" (buttonVoice) is `feedback/error/bold` + `text/inverse`. "Type instead" is `interactive/secondary` + `interactive/onSecondary`, same as every other Secondary action. The earlier "both are red" reading came from a `findOne` that matched the buttonVoice's red frame first while walking the Silence variant, not the standalone `button` instance. Verified against node 13569:6030 and corrected in code.
- Matched and verified by measurement: sheet fills (4/4), title colours (4/4), all three Continue fills and their on-bold labels, the Why? label, Silence's support text, and all four header icons. Remaining known differences are size-only, not colour, and are the already-logged hand-drawn divergence: Figma's CTA labels are 24px on Error and 18px on Partial/Success where this build uses the real button master's 21px, and Figma's button widths (100/230, 93/237) vary per variant where this build is uniform.

- 2026-09-11: Two more BottomSheetVerdict fixes after review. (1) The Silence skip control was drawing an X (`M5 5l14 14M19 5L5 19`) — a close/dismiss glyph, not a skip one. Figma's `buttonIcon-Skip` holds a real `skip-forward` icon inside an `iconSlot` (Size=300); its filled 16x16 vector path was transcribed from that instance and inset 4px inside the 24px box, rather than a skip glyph being invented. (2) "Type instead" moved from the error red back to `interactive/secondary`, per the correction above.
- A square box appeared behind the skip button, caused by the colour fix earlier in this session. `ButtonIcon` renders two elements: the root `<button>` is the 56px tap target with NO border-radius, and an inner `<span class=circle>` carries the visible round fill. A plain `style` override lands on the root, so tinting it painted a 56x56 square behind the circle. Fixed with a local `.skipButton` class that keeps the root transparent and tints the circle instead; it needs the `[data-variant][data-size]` qualifiers to out-specify ButtonIcon's own `.button[data-variant='Secondary'] .circle` rule (0,3,0), and it overrides only background and color so the circle keeps its inset bezel. Generalizable lesson: passing `style` to ButtonIcon tints the tap target, not the button — any future per-instance ButtonIcon tint needs the same treatment, which is an argument for giving ButtonIcon a real tint prop.
- 2026-09-12: The Silence sheet's secondary action is relabelled "Type instead" → "Try typing instead", staying `button` variant=Secondary. Two alternatives were considered and rejected. (1) Repointing it at the home screen ("Can't talk now, go home") was rejected because the Silence sheet covers the idle screen's own `bottomContent` fallback, so this button is the *only* text path reachable while the sheet is open — swapping it for an exit would take "text fallback reachable in one tap on every voice turn" to zero taps, and the appBar X already provides the exit one tap away. (2) Matching the idle screen's wording ("Can't talk right now") was rejected because the two moments differ: on the idle screen the student self-selects out of speaking, whereas in the Silence sheet the mic or STT may well have dropped a perfectly good answer, so situation-wording there blames the student for a system miss. Action-wording stays in the sheet, situation-wording on the idle screen. "Try" is a deliberate softening to match the sheet's "No worries, this one doesn't count against you" register, accepted with a known tension: Voice-ux principle 5 frames the text fallback as accessibility rather than a lesser option, and a hedge word pulls against that. Applied in Figma to both the `bottomSheetVerdict / variant=Silence` component (13569:6032) and the built `Silence03` screen (13561:2704), and in `BottomSheetVerdict.tsx`. The `onTypeInstead` prop keeps its name — the label changed, not the meaning.

- 2026-09-12: Spacing parity pass on BottomSheetVerdict after the gap between the Silence title and its support text read too tight. Measured Figma's `variant=Silence` (13569:6057) against the rendered story in a real browser (Playwright, computed boxes — not CSS reading): the title-bottom-to-support-top gap should be 14px and was rendering at 2px, with the whole sheet 233px instead of 260px. Two independent causes, which together account for the full 12px. (1) `.textSection` had no height, so it hugged its tallest child (the 32px iconBox) at 32px; Figma's `Text section` is FILL/FIXED at 48px because it reserves room for the `Sub-title (optional)` descriptor that every verdict variant hides but never removes. Since the title is vertically centred, hugging cost 8px below it. Now `height: var(--size-space-1200)`. (2) `.appBar` set `gap: var(--size-space-100)` but wraps a single flow child (the handle is `position: absolute`), so the gap rendered nothing; Figma's app bar hugs to 64px around a 60px nav because it counts that 4px gap even though its own handle is ABSOLUTE. Carried as `padding-bottom` instead. Every box now matches Figma exactly, offset by the sheet's own 1px top border.
- 2026-09-12: Separately found and fixed while measuring the above: `button` at `size=L` rendered 48px tall against Figma's fixed 56px. Checked the whole `button` set (4871:29883) — S and M are 48 and L is 56 across every variant and state, so the base `min-height` (`size.space.1200`, the 48px tap-target floor) was already right for S/M and only L needed an override. It went unnoticed because `buttonGroup` stretches its children to the tallest sibling, so any L button next to the 56px `buttonVoice` already looked correct; only a standalone L button was short, which is why the Silence sheet's own secondary action exposed it. `min-height: var(--size-illustration-700)` borrows the same 56px token `buttonIcon`'s L circle already uses for its diameter, rather than introducing a raw value. Swept all 15 button-bearing stories after the change: every L is 56, every S/M is 48, and the vertical `buttonGroup` correctly grows to 120 (56 + 8 + 56).
- 2026-09-12: Storybook's `test-run` timed out on every attempt this session (four tries, focused runs). The Storybook dev server itself is healthy (`/index.json` returns 200 on :6006), so this looks like the vitest browser runner failing to start rather than anything about these changes. Verification fell back to direct DOM measurement and screenshots in Playwright. The story suite has NOT been run against the CSS changes above — worth resolving before the next handoff, since a11y violations now fail the run.

- 2026-09-12: The `Keyboard` node handed over (4589:37111) turned out to be an instance of a local set (3086:16936) that is Apple's stock iOS keyboard pasted into the file, not a Yummy-Knowie component. Reported before building rather than after: all 12 of its bound variables are `remote: true` from collections named "Kit" and "Colors" (`Liquid Glass/*`, `Labels - Vibrant/Primary`, `Miscellaneous/Keyboards/Glyphs - Primary`, `Accents/Blue`), where this project's own tokens live in five local collections (Responsive, Size, Color primitives, Semantic color token, Typography) — zero overlap. 70 of its 113 fills are raw and unbound, only one of which (`#ffffff`) appears anywhere in tokens.css; its font sizes are 17/18/19/23/25 against a scale of 11/12/15/18/21/28 (only 18 matches); its radii (4.6/8.5/27/100/296/1000) match nothing; and its variants are 396–420px wide against the 390px hard rule. There is no description on the set or on any of the four variants.
- 2026-09-12: Your call, after that report: build it anyway as a prototype prop, sourced from the dark iOS keyboard, with the typing screen modelled on `reference/AskforQuiz.png`. The linked iOS 26 community file (A2h9EOf0ZJUjGOY8e8qBpS) could not be read — only the Yummy-Knowie file has the Desktop Bridge plugin connected and the REST token is expired (403) — so the local set was used, which is already the dark iOS keyboard and already binds `Liquid Glass/*`.
- 2026-09-12: `Keyboard` (app/components/Keyboard.tsx) built as DOM, not as an exported asset. The asset route was tried first because it would have introduced no raw values at all (the same route `public/images/` already takes for the mascot), but exporting all four variants times out even at 30s thanks to the ~420px Emoji grid; a single `Default` SVG exports fine at 56KB, so that route stays open if only one variant is ever needed. Apple's values are therefore hardcoded, quarantined in `Keyboard.module.css` behind a header block that says what they are, why they're there, and that they must never be copied out or promoted into tokens.json. The component is `aria-hidden` with inert `<span>` keys — it is a picture, not an input. Its axis is `type` (Figma's own `Type`: Default/Search/Numbers/Emoji), the one component in the library not using `variant`/`size`/`state`, since the axis belongs to Apple's kit.
- 2026-09-12: Several keyboard details follow the real app screenshot you supplied rather than Figma's Apple kit, which is a generic iOS keyboard and doesn't match what Knowunity renders: uppercase letters (Figma has lowercase), a four-key bottom row (`123` · emoji · space · send) where Figma has three, a send arrow rather than a return glyph on the blue key, and a globe rather than a second smiley in the accessory row. Two further deliberate divergences: `123` on the Default variant where Figma's shows `ABC` (wrong on any real device, and would read as a bug), and a system-UI font stack rather than `font-family/default`, since this surface is drawn by iOS in SF Pro and there is deliberately no token for a platform font. Key fill stays Figma's transcribed `rgba(153,153,153,0.15)` even though the shipped app's keys look slightly lighter — eyeballing a new colour off a screenshot would be inventing a value.
- 2026-09-12: `AnswerInput` (app/components/AnswerInput.tsx) built to fill the gap the keyboard investigation exposed: a sweep of the whole Figma file (14 component sets, 5 standalone components) found no input, text field, textarea or search bar of any kind, so "Try typing instead" led nowhere. Fully token-bound — `background/input` and `text/disabled` both turned out to have descriptions naming this exact use, and nothing else in the library had claimed either yet. Its `state` axis (Empty/Filled/Focused/Error/Disabled) is defined ahead of Figma, using `state` rather than `variant` because `transcriptDisplay` already uses that axis for the same kind of surface; same class of ahead-of-Figma call as `mascotSlot`'s `expression` and `buttonVoice`'s `ctaText`, and it needs reconciling once the component exists in the file. The trailing control swaps rather than stacks: Empty rests on the mic (the way back to voice), and send takes that slot once there's text. The shipped composer's `+` and camera are deliberately omitted — attaching a photo isn't an answer to a spoken-recall prompt.
- 2026-09-12: Known a11y finding, NOT fixed, awaiting your call: `Keyboard` at `type=Search` fails axe colour-contrast (serious). Its blue key carries the word "search" in white on Apple's `#0091ff`, which measures ~3.2:1 against the 4.5:1 needed at 18px regular. The other three variants pass, because their blue key holds an SVG glyph rather than text. Every option is bad in a different way — recolouring invents a value that is neither Apple's nor the design system's, and the real iOS keyboard has exactly this contrast — so it is logged rather than silently restyled. Separately, `region` violations reported by a raw axe run are a false positive: they fire on existing known-clean stories (`bottomSheetVerdict--silence`, `transcriptDisplay--empty`) too, because Storybook's a11y addon excludes that rule.
- 2026-09-12: Storybook `test-run` still times out (every attempt this session, now seven). Both new components were verified instead by rendering each story in Playwright and running axe-core directly against it — which is how the contrast finding above was found at all.

- 2026-09-12: RESOLVED, your call: the `Keyboard` `type=Search` colour-contrast failure is suppressed, scoped to that one story and no wider (option 1 of three). `color-contrast` is disabled via that story's own `parameters.a11y.config.rules`, with a comment recording the measured ~3.2:1, why recolouring was rejected, and an instruction not to widen it to the other three variants, which pass and should keep having to. Verified the suppression is doing the work rather than passing by coincidence: re-enabled the rule and ran the file — `Type=Search` failed on `color-contrast` and the other three passed — then restored it and got 4/4.
- 2026-09-12: Diagnosed why Storybook's MCP `test-run` timed out on every call this session. **The test suite itself was never the problem**: run directly as `npx vitest run --project=storybook` it passes 83/83 (22 files) in ~9s wall clock. `test-run` doesn't spawn Vitest — it hands the run to the dev server's *embedded* Vitest worker (`@storybook/addon-vitest/dist/node/vitest.js`, a child of `storybook dev -p 6006`). That worker had been alive for 13h16m (spawned Fri 21:31) and sat at 0.0% CPU with its headless Chromium also idle, i.e. alive but not processing requests. Reading `addon-vitest/dist/preset.js` shows why nothing recovered it: `runTestRunner` only boots a worker when none exists (`child || bootTestRunner(...)`), so a wedged-but-running child is reused indefinitely and never replaced; and the run responder only replies on `TEST_RUN_COMPLETED`, `FATAL_ERROR` or `CANCEL_RUN`, none of which a wedged child emits. The server-side wait is 30 minutes (`DEFAULT_TEST_RUN_TIMEOUT_MS`), far past the MCP client's own timeout — hence "The operation timed out" rather than an error. Every call timing out (never "Tests are already running") points at the hang being at `store.untilReady()`, before a run is ever marked in flight. **Fix: restart `storybook dev`, which spawns a fresh worker.** If `test-run` hangs again, check the worker's age and CPU first (`ps -o pid,stat,%cpu,etime` on the `addon-vitest/dist/node/vitest.js` process) before suspecting the tests.
- 2026-09-12: The reference supplied for this (storybook.js.org/addons/test-runner-with-snap-speed-exp2) describes `@storybook/test-runner` — the older Jest + Playwright runner, Storybook 8, `^0.17`. Not adopted: this project is on Storybook 10.6 with `@storybook/addon-vitest`, that runner's successor, and the suite already runs correctly under it. Adding test-runner would stand up a second, parallel test system to route around a stale process rather than fix it.
- 2026-09-12: Two unrelated warnings noticed during the CLI runs, not acted on: Vite warns `vitest.config.ts` uses ESM syntax in a file loaded as CommonJS (cosmetic today, but `configLoader: 'native'` is slated to become Vite's default, so rename to `.mts` or set `"type": "module"` before that lands); and some story passes a boolean to a `color` control ("Control of type color only supports string, received boolean").

- 2026-09-12: Fix confirmed. At your go-ahead, the stuck `storybook dev` (up 13h21m) was stopped with SIGTERM; its own shutdown handler took the wedged Vitest worker and its headless Chromium down with it within ~1s, leaving no orphans and port 6006 free. A fresh `npm run storybook -- --no-open` came up immediately. The first MCP `test-run` after that returned in seconds — the first one to complete all session — and a full-suite `test-run` then passed all 83 stories (22 files, including both new components) with a11y in `error` mode. Note the restarted server now runs as a background process from the Claude Code session rather than in your own terminal, so its output goes to a log file, not your screen, and it may stop when that session ends; if 6006 is down later, `npm run storybook` in your terminal brings it back.

- 2026-09-12: Fixed `Keyboard` and `AnswerInput` stories running off the screen. Nothing inside either component overflowed — the stories' own wrappers did: the preview defaults to the 390px `mobile390` viewport, both files used `layout: 'centered'` (which pads every story 16px) around a fixed `width: 390px` decorator, so each story measured 422px and ran 16px past the right edge. Switched both to `layout: 'fullscreen'` with `width: 100%`, the same call `Screen`'s stories already made. A second, vertical bug surfaced once that was fixed: Storybook wraps story-level decorators inside meta-level ones, so `AnswerInput`'s padded meta decorator also padded the full-height typing screen and pushed its keyboard 16px below the bottom edge. The padded wrapper moved from `meta` onto the five state stories individually. Measured at 390×844 afterwards: all 10 stories span exactly 0→390 with no scroll in either direction, the typing-screen keyboard ends at exactly 844, and all 10 pass `test-run`.

- 2026-09-12: Keyboard is now always flush to the bottom edge in Storybook. `Keyboard`'s four stories previously sat at the top of the canvas (only the typing-screen story was bottom-aligned); their decorator now uses `minHeight: 100dvh` with a column flex and `justifyContent: flex-end`, matching the typing screen. `dvh` rather than `vh` so it tracks the visible viewport on a phone. Verified at four phone heights (568, 667, 844, 932) across all five keyboard-bearing stories: keyboard bottom equals viewport height in every case, with no vertical scroll. All five pass `test-run`.
- 2026-09-12: Bug found in `AnswerInput` while stress-testing the above, NOT fixed yet: the field does not grow. A 1,439-character answer left the bar at 82px, identical to empty. The textarea is `rows={1}` with only a `max-height` of four Body M lines, and `max-height` caps growth but never causes it, so the text scrolls inside a one-line box instead. The CSS comment and the component's Storybook docs both claim it grows to four lines before scrolling; that claim is currently false. The keyboard stays flush either way — the bug is the input's, not the layout's.

- 2026-09-12: RESOLVED, your call: `AnswerInput` now grows one line at a time up to four lines of Body M, then scrolls, and shrinks back as text is deleted. Growth is re-measured from `scrollHeight` in a layout effect; CSS `field-sizing: content` would be one line but isn't supported in Safari, and this is an iOS prototype. Measured while typing at 390×844: 1 line = 24px, 2 = 48, 3 = 72, 4 = 96, and lines 5–7 hold at 96 with the text scrolling. Two further bugs surfaced and were fixed in the same pass. (1) The first measurement was 32px per line, not 24: the textarea kept the browser's default 2px top/bottom padding, which `scrollHeight` includes but a content-box `height` doesn't, so it was counted twice (and the cap landed at 100px). Fixed with an explicit `padding-block: 0`. (2) Send never appeared when typing into an uncontrolled field: the mic/send swap read the `value` prop, which is undefined when nothing passes it in — so the typing-screen prototype showed the mic even with a full answer typed. The field now keeps its own draft and defers to `value` only when given. Verified: mic before typing, send after one character, mic again for whitespace-only or cleared text.
- 2026-09-12: `AnswerInput`'s pill radius moved from `radius/full` to `radius/900` (36px). At four lines, `full` made both ends semicircles that ran into the first and last lines of text. At one line the field is 66px tall, so 36px already exceeds half the height and is clamped to the same shape — confirmed by screenshotting the one-line field with each radius and comparing the PNGs byte-for-byte (0 differing bytes).
- 2026-09-12: Added a `Typing grows to four lines` story whose play function types into an uncontrolled field and asserts the mic/send swap, the height at 1, 3 and 6 lines against Body M's line height, that it scrolls past four, and that clearing resets both. Checked that it catches the bug rather than passing regardless: with the layout effect commented out it failed (`expected 24 to be 72`), and passed again once restored. The keyboard stays flush at the bottom with the bar at full height (checked at 568 and 844). Full `test-run`: all 84 stories pass.

- 2026-09-12: CORRECTION to the `AnswerInput` entry above. It says a sweep of the Figma file "found no input, text field, textarea or search bar of any kind". That was wrong: a `Text Field` component set exists at 4517:2132, directly on the "🎨 Mascot & components" page. Last session's sweep of that page listed eleven sets and not this one; today the same query returns it. Can't tell from here whether it was off-page then and has since been placed, or the sweep missed it — but the claim overstated what a page sweep can prove either way, because a component with no parent page is invisible to one, and three such orphaned sets exist right now: `Keyboard` (3086:16936), `Icon Slot` (3227:24902) and `Button icon` (4095:15732). Corrected in `AnswerInput`'s Storybook docs and `design-system.md`. `AnswerInput` is not built from `Text Field`.
- 2026-09-12: Asked to build `Text Field` (4517:2132) as a React component. Stopped before writing any code, per your rule that a missing value means stop and tell you. Findings, all read from the live file: (1) **No description** on the set or any of its three variants (`Variant` = Default / Error / Placeholder), so there is nothing to transcribe into the story docs. (2) **Two colour bindings point at variables that no longer exist in the token collections.** The field fill binds a `background/input` (VariableID:3209:8131) and the error stroke and error caption bind `feedback/error` (VariableID:3209:8162); neither ID is in the file's current collections. The live `background/input` is a different variable (navy-900, `#1a1c26`), and `feedback/error` has since split into `bold`/`onBold`/`subtle`/`onSubtle` with no plain `feedback/error` left. The stale fill still renders as 10% white, so the component doesn't look the way its token names suggest. A third stale binding, `Illustration/400`, sizes the trailing button. (3) **Typography is unbound and off-scale.** All text is Inter Variable (tokens.json has only Greed), set at 14px SemiBold for title and input (no 14px anywhere in tokens.css) and 11px Regular for captions, with AUTO line height (no token for that), and no text style applied. (4) **It nests the two orphaned duplicates instead of the library's own components**: the leading icon is `Icon Slot` (Size XXS/XS/S/M) rather than `iconSlot`, and the trailing clear control is `Button icon` (`Variant=Subtle`, which `buttonIcon` has no equivalent for) rather than `buttonIcon`. That's the two-definitions split `design-system.md`'s never-list forbids. (5) Smaller: the 1px stroke weight is unbound (`size.stroke.border` matches it exactly); the Default variant's caption text isn't exposed as a property, so it can't be changed from an instance; the field frame is named "Error" in all three variants; and `Placeholder` is modelled as a variant, where it's really the empty state of `Default`. Storybook's MCP server was also down this session (the dev server restarted last session ran from that session and stopped with it), so the library was checked by reading the component and story files directly.
- 2026-09-12: "Why?" on the verdict sheet (Success, Partial, Error — Silence has no Why?) opens an explanation sheet modelled on the shipped quiz's own post-answer explanation: a bottom sheet with a short paragraph explaining the answer, key terms in bold, the mascot peeking over the sheet's top edge, and a verdict-coloured "Got it" pill. Your call, from a screenshot of the shipped quiz, saved as `reference/Quiz-Why-explanation.png`. Not designed in Figma yet.
- The shipped sheet also ends in a "How can I help?" chat input. That is NOT carried over: an open question box after an answer is the tutoring/conversation branch the brief's hard constraints and CLAUDE.md's never-list rule out. Recorded as an exclusion forced by an existing rule, not a new preference.
- Open, not decided: (1) where "Got it" goes — back to the verdict sheet, or straight on to the next term; (2) whether Why? on an Error verdict shows the full answer, which would reveal it before the kickoff's hint → re-attempt → reveal ladder runs, and runs against the kickoff's "explanations held back until the end" (both are kickoff thinking, open per the brief's mandate, but the conflict should be a conscious choice); (3) the peeking mascot is a small, inline mascot appearance, which design-system.md says `mascotSlot` doesn't support below 2XL.
- Thumbs up/down on the verdict sheet is out of scope this sprint. The control stays in the Figma component but isn't designed further or wired to anything; its missing selected state (logged above) is no longer an open gap for this sprint.
- The exam-tab `badge` is dropped from the voice-ux gap tracking, at your direction. Its entry in design-system.md is unchanged.
- The "Got it" pill on the Why? explanation sheet is out of scope this sprint, at your direction. Open question (1) above — where "Got it" goes — is therefore no longer open for this sprint. Not a decision about the sheet itself: CLAUDE.md requires every required action to have a way out, so when the sheet is designed its exit still has to come from somewhere else (for example the sheet's own dismiss control), rather than from "Got it".

- 2026-09-12: `TextField` (app/components/TextField.tsx) built from Figma's `Text Field` set (4517:2132), after the stop above, using the token mapping you approved: fill = live `background/input`; error stroke = `border/error`; error caption = `text/error`; title and input = `Body S Bold`; captions = `Caption S Regular`; nested `Icon Slot` / `Button icon` = the library's `iconSlot` (300) and `buttonIcon` (Tertiary, S). Props keep Figma's names: `variant` (Default/Error/Placeholder, default `Error` for parity — a known trap, flagged in the docs), `showTitle`, `showLeadingIcon`, `showTrailingIcon`, `showCaption`, `titleText`, `placeholder`, `errorCaption`; added ahead of Figma `value`/`defaultValue`/`onChange`, `onClear`, and `caption` (Figma's Default caption isn't an exposed property). Two further calls made during the build, both same-pixel or strictly better: the search icon takes `text/primary` rather than the `background/inverse` Figma binds (identical colour, but that token is a surface fill), and the clear icon is light because `buttonIcon` Tertiary paints `text/primary` — Figma's `interactive/onPrimary` dark navy only reads on the white canvas and would all but vanish on the dark field. A focus edge (`border/focus`) is added where Figma has no focus state, except on Error, which keeps its red edge. The stroke is an inset shadow rather than a CSS border, because Figma strokes inside: a border made the field 50px against Figma's 48px, now exactly 48 (and 334px wide at 390, matching). The typography mapping's line heights leave two small height differences, both documented: 44px vs 41 with the clear button hidden, and Error at 62px vs 63. Every `var()` in the stylesheet resolves in tokens.css; no raw hex or pixel values outside comments. Stories: `Variant=Default`, `Variant=Error`, `Variant=Placeholder`, `showTitle=true`, `showCaption=true`, plus a `Clear empties the field` interaction test — confirmed to catch a broken clear button (fails with the empty-on-clear line commented out, passes restored). Storybook's MCP server was down this session, so tests ran through the same Vitest runner directly: all 90 stories pass with a11y in `error` mode. Storybook was restarted in the background from this session, so it will stop when the session ends.

- 2026-09-12: Cross-references updated now that `textField` exists. `Keyboard`'s Storybook docs and its `design-system.md` entry both said "if you want a text field, that's `answerInput`", and `answerInput`'s own guidance didn't point anywhere for ordinary form fields. All now split the two: `answerInput` for the recall answer, `textField` for single-line form fields.
- 2026-09-12: `TextField` typography changed at your direction: title, input and placeholder move from `Body S Bold` to `Body S Regular`. Captions stay `Caption S Regular` (11px), unchanged. Same size and line height as before (15px / 20px), so no layout or height changes; only the weight drops from semibold to regular. Updated in the stylesheet, the component's Storybook docs and its `design-system.md` entry.
- 2026-09-13: `bottomSheetVerdict` renamed to `resultBtm` at your direction: `ResultBtm` in code (`app/components/ResultBtm.tsx`, `.module.css`, `.stories.tsx`; Storybook title `Components/ResultBtm`, so story IDs are now `resultbtm--*`) and `resultBtm` in `design-system.md` and cross-referencing story docs, keeping the camelCase naming rule. `Result-btm` was considered and not used, since the hyphen breaks that rule. Earlier entries in this log keep the old name as written. In Figma the set (13562:5922) had already been renamed to `ResultBtm`; by your call it is now `resultBtm` to match the camelCase rule, and its section (13562:5777) is `resultBtm (local, verdict states after a student answers)`. Variant names unchanged. A search of every page found no other layer carrying the old name. Figma's plugin couldn't connect at first because a leftover figma-console-mcp server from a closed session held the plugin's connection on port 9223; stopping it let this session's server take over. By your call, the Storybook label (`Components/ResultBtm`) and file names (`ResultBtm.*`) stay capitalised to match the code name, the same split every other component uses (Figma `bottomSheet`, code `BottomSheet`); the code name itself can't be lowercase, since JSX treats a lowercase tag as an HTML element.

- 2026-09-14: Recall loop, failure paths and mock engine decided in a design interview. Built for **moderated usability tests with English-speaking students**, so the mock is tuned for believability, not for steering a demo.

### Mock engine
- Transcription uses the browser's built-in recognizer (`webkitSpeechRecognition`) with a real mic (`getUserMedia`). Judging stays mocked. This changes CLAUDE.md's "never build real STT" rule to "no custom STT engine", updated the same day. Chosen over a facilitator typing live because a human can't keep pace with a live transcript, and because real mis-hearings are what the "heard wrong vs didn't know" principle needs to be tested against.
- The judge is a keyword judge running on the device, plus a turn log. Each question is authored with 3 key concepts (with synonyms and likely mis-hearings). 2+ concepts in a real sentence = Pass, 1 = Partial, 0 = Fail, empty transcript = Silence. A facilitator verdict was rejected: it needs a second device, a sync service and session pairing.
- Partial needs at least one concept. The earlier "any on-topic attempt is Partial" floor was raised, because combined with one attempt per term it would have made Fail almost unreachable.
- Keyword stuffing ("chlorophyll, glucose, oxygen") caps at Partial: a Pass needs sentence structure, not a list.
- Question detection is loose: any question pattern ("what's…", "can you…", "I don't get…") shows a snackbar redirect over the idle screen ("can't answer questions here, give it your best shot") and uses no attempt. Accepted cost: a real answer starting with "What happens is…" can be swallowed, costing the student a re-take.
- Latency is faked on top of the recognizer: usually 2–4s, sometimes 7–8s, so the slow state is exercised naturally. Browser STT alone returns in ~1s, which would leave the brief's wait constraint untested.
- iOS recognizer quirks: if it ends its own session on a pause while the student is still recording, it silently restarts and shows a "still listening" cue; only the student's send ends a take. If no final result arrives after send, the last interim transcript is judged; if that's empty, the Silence path runs.
- Recognizer/network errors after send use the Silence path (Record again / Try typing instead, no penalty). Known copy mismatch: "didn't catch that" blames audio for a network fault. Accepted to avoid a new state.
- Noise with an empty transcript is Silence, same as quiet.
- Takes under ~1s with an empty transcript (a start/send fumble) are dropped silently, back to idle, no sheet.
- The turn log (transcript, concepts hit, verdict, latency, modality) is stored on the device and read on a hidden `/log` route with copy-as-CSV. It never appears in the student flow.
- Research consent for minors' audio going to Apple's recognizer is handled by the study consent form, not in-app.
- To do before building on browser STT: spike `getUserMedia` + `webkitSpeechRecognition` on the test iPhone, in both a Safari tab and home-screen (standalone) mode. The plan is to run as a home-screen web app, to avoid Safari re-prompting for the mic on every load, but speech recognition has historically been less reliable in standalone mode.

### The loop
- **One attempt per term.** No hints and no retry within a session: the kickoff's hint → re-attempt → hint → reveal ladder is cut. After any verdict the sheet offers Why? (explanation) and Continue (next term). Say it back is out this sprint.
- A mis-heard answer's verdict stands once sent. The safety net is the **live transcript while speaking plus cancel before send**: the student sees the mis-hearing as it happens. A deliberate tradeoff against "a false wrong is demoralizing", softened by the generous judge.
- **Transcript streams live while speaking.** When it passes transcriptDisplay's 320px window it auto-follows the newest line with older lines fading at the top; after send it becomes scrollable for review behind the sheet.
- **Recording controls:** `buttonGroup` of a Secondary `buttonIcon` (cancel/discard) and `buttonVoice` (send), matching the beta layout. "Can't talk right now" and Skip are **hidden while recording**; cancel returns to idle, where both reappear. This bends "one tap to text on every voice turn" during recording only; CLAUDE.md reworded the same day.
- **Skip** sits in the bottom area next to the typing link on the idle screen.
- **Waveform:** the Phosphor waveform icon (phosphoricons.com, "wave"), placed inside `buttonVoice`, shown only while recording. This closes the "icon swap not wired" gap on buttonVoice. Deliberate mismatch, to flag with Harry: the rest of the library's icons are a different family.
- **Processing** shows no waveform. A `mascotSlot` at XL is on the loop screen and swaps expression (e.g. standby → thinking). First real XL use; design-system.md's mascotSlot entry updated to allow it for in-loop status.
- **Prompt** is a full question ("What happens to particles during diffusion?"), like the shipped quiz, not a bare term. It's clearer scope for a keyword judge.
- **Interruptions** (call, backgrounding, lock) discard the take and return to idle on the same term with a snackbar; no attempt used.
- **X is allowed during processing**: the answer is discarded.
- Silence has no cap on repeats; Skip and typing are always one tap away on that sheet.

### Why? sheet
- Why? follows `reference/TapWhy?.PNG`: an explanation bottom sheet that overlays the screen, with the student's transcript staying behind it, the mascot peeking over the sheet edge, and a "Got it" pill. **"Got it" is back in scope** (it was cut 2026-09-12) and always goes to the next term, on Success, Partial and Error alike.
- One authored explanation per question. The concepts the keyword judge found missing are set in bold, so a Partial sees exactly what to add.
- Not dismissible by drag or scrim; Got it is the only way out, and it's always available.
- Got it is `interactive/primary` on every verdict, not verdict-coloured, because the explanation isn't a verdict and shouldn't repeat a miss's red.
- The single-sheet model resolves both earlier open questions: explanations are revealed on Why?, and there's no hint ladder left to conflict with.

### Text fallback
- Typed answers go through the identical judge (thresholds, stuffing cap, question detection), and the summary doesn't label modality.
- Every term opens on voice again, even after typing on the previous one, to protect the experiment.
- With the keyboard up, the question collapses to one line (tap to expand). Skip is reached by dismissing the keyboard (two taps).

### Mic permission and the toggle
- The primer fires when the Voice recall toggle turns on, as one bottomSheet that does both jobs: it explains active recall and asks for the mic. It has "Turn on mic" (triggers OS prompt) and "Not now". It replaces the kickoff's separate first-run screen.
- "Don't Allow" snaps the toggle back off. Turning it on again later shows the primer with Settings steps and an "I've turned on the mic" re-check, since iOS won't show its prompt twice.
- If permission is lost later, each term's mic tap shows a "Turn on mic in Settings" sheet with Type instead (primary) and Skip (secondary). There's no Open Settings button, because a web app can't deep-link there.
- The toggle is locked while inside a voice node and unlocks on exit. Since the toggle isn't reachable from inside the loop and sessions restart on leave (below), this is effectively no lock.

### Session and after
- **Always 4 terms**, so progressIndicator's 0/25/50/75/100 snaps map exactly. Kickoff's 3–5 range narrowed.
- **Leaving restarts the session**, with no confirmation on X. This reverses the kickoff's "progress saves, returning resumes", accepted because a 4-term session is short. Turning the toggle off discards nothing extra, since nothing is saved.
- **Summary counts:** "explained unaided" = Pass in any modality; Partial and Fail are separate; Skip is separate. No confidence rating. Honest counts only. This leaves the brief's "overconfidence must cost / underconfidence rewarded" requirement unmet, as a known gap.
- **Summary actions:** Continue (primary) and Try again (secondary), which reruns only non-pass terms with no top-up (progress uses the nearest snap). A caption says it plainly: "Try the 2 you missed now, or review them later." Try again counts as a term's second and final round.
- **After:** the node completes whatever the result. Non-pass terms queue for a review node later in the path, topped up to 4 with previously passed terms. A term leaves the system after its second round, pass or fail. The student learns about the queue from a summary line only; the path shows the review node when it's due. Scheduling is mocked.

### Cut or open from this interview
- XP: out of scope this sprint (brief open question 2 stays open).
- Accessibility of the live parts (VoiceOver announcing streaming transcript, reduced motion for the waveform and auto-follow scroll): out of scope, known gap.
- Hints and Say it back: cut.

- 2026-09-14: SPEC.md Open list, first pass (missing props and components, hosting, content).
- **Waveform** goes in `ButtonVoice`'s Recording state as a **left icon with the "Send" label**, using Phosphor's `Waveform` (`@phosphor-icons/react`, already a dependency through `Snackbar`). Chosen over `button`'s Center Icon Container: in Figma that layer is only switched on in the Loading state, where it replaces the label (icon-only), and it isn't exposed as a property. The left icon container is already exposed as `showLeftIcon`, and `Button` already has `leftIcon`, so the only code change is letting `ButtonVoice` pass it through. Pairs the icon with a label, per Voice-ux principle 1. The Phosphor-vs-library icon mismatch is smaller than first logged: Phosphor is already used in code.
- **Long answers** use `transcriptDisplay` state=Overflow (Figma 13563:1611: 320px clip window, 96px `size.space.2400` padding), **anchored to the newest text** so the latest words stay visible while speaking. This is an alignment change inside the component (today it clips from the top, so new words fall below the window) and should be matched in Figma. It replaces the earlier "follow the newest line with a top fade" idea; there is no fade.
- **Snackbar:** the only snackbar in this flow is the exam plan's readiness banner (Figma 13547:1235, "Voice recall is ready for Biology. Try say it out loud." + Show me). The in-loop snackbars decided earlier are dropped:
  - **Question redirect** now shows the `ResultBtm` Silence sheet (Re-record / Try typing instead / Skip), no attempt used. Known copy mismatch: Silence reads as "didn't catch that", not "can't answer questions".
  - **Interruption** (call, lock, backgrounding mid-recording) returns to idle with `TranscriptDisplay` state=Silence ("Sorry, I didn't catch that. Can you repeat?"), no attempt used.
- **appBar** will be built from Figma's `appBar` set (9003:8606) after the Open list is worked through, and used as the loop's top navigation.
- **Exam plan path** already exists in Figma (13547:1235) and is out of scope for the build for now.
- `ResultBtm`'s thumbs up/down stay rendered and unwired.
- **Hosting:** a tunnel (Cloudflare quick tunnel or ngrok) or local HTTPS (`next dev --experimental-https`) while building and for the spike; Vercel for participant sessions, where a stable URL keeps the home-screen install working. Vercel needs a GitHub remote, which the repo doesn't have yet. Neither tunnel tool is installed yet.
- **Content:** a draft of 8 middle-school biology questions (Eukaryotic and Prokaryotic Cells, 4 per node, matching the node names on the Figma exam plan) is in `content/voice-recall-questions.md`, each with 3 concepts, synonyms, guessed mis-hearings, an explanation and sample answers. Q5 reworded to ask *where* the DNA is, because a keyword judge can't read "has / doesn't have a nucleus". Q6 reworded to ask *why* plants need a cell wall and chloroplasts, because a naming question would be capped at Partial by the bare-list rule.

- 2026-09-14: SPEC.md Open list, second pass. It works from the existing Figma flow in section "Design for voice recall first run experience" on the "Design the core flow" page: Homescreen 13619:3109 → Exam00 13548:6324 → Hint-animate01 13547:5824 → Turnon02 13548:6325 → gate 13555:8294 → Starting04 13548:6327 → Talking05/06 → Talking-finished07a → Thinking 13642:7889 → End09, plus Partialright 13549:6911 and Section 2 13556:4495 for the Partial and Error sheets.

### Entry and first run
- **The prototype starts on the Homescreen** (13619:3109). The exam plan screens (Exam00, Hint-animate01, Turnon02) are **static images with tap zones**: exam tab → Exam00, Show me → Hint-animate01, Voice recall toggle → Turnon02, a node → the gate. No exam plan components are built. This replaces "the primer is a bottom sheet on toggle-on".
- **The mic primer is the full-screen gate** (13555:8294): "Say it, don't just tap it", with **Turn on microphone** and **Can't talk right now**. **Turn on microphone** triggers the real iOS prompt directly. The gate's invented "Allow microphone access?" sheet is **dropped from the build**: with a real mic, iOS shows its own prompt right after, so students would be asked twice in a row. The gate already does the priming Voice-ux asks for.
- **Don't Allow at the gate** → back to the exam plan image. Returning later shows the gate with steps for turning the mic on in Settings.
- **Review is cut this sprint**: no queue, no review node, no "comes back later" line. Try again on the summary is the only second round. This supersedes the review-node decisions above.

### Loop screen (Starting04 onward)
- **Question** sits in a speech bubble beside the mascot, with `aiDisclaimer` under it, as in Figma. The bubble is hand-drawn in Figma and is built **inline in the loop screen**, not as a component.
- **Mascot is `mascotSlot` XL (64px).** The Figma screens use a 2XL instance resized to 84px, which matches no size or token; fix those instances to XL in Figma. The XL exception in design-system.md stands.
- **Idle bottom** follows Figma 13561:2721: `buttonGroup` (Horizontal, L) of a Secondary `buttonIcon` Skip and `buttonVoice`, with a Secondary `button` "Can't talk right now" (L, full width) below.
- **`buttonVoice` icons** use its left icon slot (Figma's exposed `showLeftIcon`): Phosphor `Microphone` + "Start" at idle, Phosphor `Waveform` + "Send" while recording.
- **Recording:** the `buttonIcon` in the same group swaps from Skip to a cancel/discard icon, and cancel returns to idle. Figma keeps Skip there during recording; update Figma to match, because Voice-ux lists re-record before send as a Must.
- **Still listening:** when iOS restarts recognition mid-take, a brief "Still listening…" line in `text/tertiary` appears under the transcript for about 1.5s. Plain text.
- **Processing** follows the Thinking frame (13642:7889):
  - `buttonVoice` is **hidden**.
  - The bubble swaps the question for timed phrases: "Let me think…" at 0s, "Checking your answer…" at about 2s, "Almost there…" at about 5s, holding until the verdict. This replaces the separate "taking a moment" line.
  - The transcript stays.
  - The mascot (thinking expression) gets a **CSS motion loop** (transform only, static under reduced motion).
  - Figma's Thinking and Loading08 frames still show the Stop button; update them.
- **XP chip** ("⚡2") appears in the top nav as **static**, never counting. XP stays out of scope.
- The orphaned `bottomCta` set (5101:6963: no page, no description) wraps the same [buttonIcon | buttonVoice] pair. The build uses `buttonGroup` directly. Flag it for deletion or reattachment, per design-system.md's never-list.

### Why? sheet
- Composed as `BottomSheet` + `BottomSheetAppBar`, with the peeking `mascotSlot` and the "Got it" `Button` **positioned outside `BottomSheet`** in `Screen`'s `bottomSheetOnly` slot, to match `reference/TapWhy?.PNG`. No component change; the positioning is custom.

### Summary
- **A `BottomSheet` at height L over question 4's screen** (90dvh cap), rows scrolling inside `middleSection`.
- **Layout:** each row shows the question, a **snippet of the student's own transcript**, and the verdict, so the count is backed by their words. Rows are built inline in the summary, not as a component.
- **Try again caption:** "One more try at the 2 you missed".
- **After a Try again run:** the summary shows **only the rerun questions**.

### Typing turn
- **The whole typing turn is out of scope this sprint.** `AnswerInput` and `Keyboard` stay in the library, unused by this flow.
- **"Can't talk right now"** (idle screen and gate) **goes back to the previous screen, the exam plan image Exam00** (13548:6324). On the idle screen that leaves the session, which restarts next time, as with close.
- "Try typing instead" (Silence sheet) and "Type instead" (mic-off sheet) lead to a placeholder screen saying typing isn't in this prototype, with Skip and back to voice. Open: whether these should also go back to Exam00.
- CLAUDE.md's text-fallback rule reworded to match.

### Mock engine
- **Real sentence** (needed for a Pass): at least 6 words and at least one linking word (is, are, it, they, has, have, makes, uses, because, so, and, which, that).
- **Question detection:** the transcript opens with what, what's, how, why, can you, could you, is it, does it, I don't get, I don't know what, what does or wait, or ends with "?". The redirect is the `ResultBtm` Silence sheet.
- **Slow turns:** about 1 in 5 turns are slow (7–8s) at random. URL flags force states for testing: `?latency=slow`, `?latency=hang`.
- **Turn log:** cleared with a "Clear log" button on `/log`, with a confirm, after copying the CSV.

- 2026-09-14: Figma follow-ups applied to the Yummy-Knowie file.
  - **Mascot:** `mascotSlot` set to XL (64px, resized down from 84) and re-seated on its shadow on 12 loop screens: Starting04, Starting, Talking05, Talking06, Talking-finished07a, Loading/AnimateHowie08, Thinking, End09, Partial08b, Incorrect, Silence03 and VoiceSignal03.
  - **Overflow:** `transcriptDisplay` state=Overflow (13563:1611) vertical alignment CENTER → MAX (bottom), and the Overflow sentence in the set's Figma description rewritten. Figma clips at the 512px frame edge, so long text shows into the 96px padding before it's cut; the React build clips at the 320px window.
  - **Processing:** `buttonVoice` hidden on Thinking and Loading/AnimateHowie08. The Skip `buttonIcon` still shows there, by your choice; what Skip does during processing is open.
  - **Discard icon:** Phosphor ArrowCounterClockwise on Talking05/06/07a, replacing `skip-forward`. x-close was tried first and rejected because it duplicated the top-left close X.
  - **Phosphor icon components:** "microphone (phosphor)", "waveform (phosphor)" and "arrow-counter-clockwise (phosphor)" (regular weight, 24px, fill bound to `background/inverse` like the existing icons) created in a new section "Phosphor icons (voice recall)" on the components page (13646:8537). The file's own icons are Untitled-style and have no waveform; Phosphor was imported so Figma matches code. Icon-family mismatch still flagged for Harry.
  - **buttonVoice master:** Default shows mic + "Start", Recording shows waveform + "Stop" (the label change to "Send" is deferred, your call). On the 13 loop screens that restyle `buttonVoice` light (`background/inverse` fill), the icon fill is overridden to `text/inverse` to match the label, or it was invisible. Talking05/06/07a still use state=Default with a "Stop" text override, so they show the mic icon while recording until the state/label change is made.
  - **bottomCta:** the 9 uses on "Design the core flow" were detached into `bottomContent` frames holding the real `buttonGroup` instance, with padding bindings and overrides kept. The set (5101:6963) is **not deleted**: 4 uses remain on "✨ Example Screens", 1 on "TrashCan", and 1 loose instance on the components page (13646:8210) that you placed. Deletion is deferred.

- 2026-09-14: `AppBar` (app/components/AppBar.tsx) built from Figma's `appBar` set (9003:8606).
  - **Props:** `variant` with Figma's exact six options (default / leftIconButtonOnly / leftAndRightIconButton / leftAndRightButton / leftAndTwoRightIconButtons / leftAnd2RightButtons), `slot` for Figma's Slot, and icon/label/click props for each button.
  - **Built from existing components:** `ButtonIcon` (Tertiary, M) for every icon button, the same reuse `BottomSheetAppBar` made for the orphaned `App Bar Button Icon`, and `Button` (Tertiary, S) for the text button, standing in for the orphaned `App Bar Button`. Its differences are documented in the story: 20px vs 16px line height, no pressed dim, and a 48px minimum width vs Figma's 30px.
  - **Missing values, decided:** no fixed height (Figma's unbound 56px comes from the 48px buttons plus `space.200` bottom padding); the Slot's unbound 10px padding and gap become 0 and `space.300`, rebound in Figma (Space/0, Space/300) on all six variants; the unbound 375px width becomes fluid.
  - **Background:** Figma's two stacked `background/page` → transparent gradients are both reproduced.
  - **Icons:** Phosphor defaults by decision: ArrowLeft, DotsThreeVertical and Export stand in for arrow-left, dots-vertical and share-02. ShareNetwork was used first and swapped for Export, which is the matching box-with-arrow share glyph.
  - **Chips beside the progress bar** go inside `slot`; no second slot prop.
  - **Stories:** one per variant, named `variant=…`, plus "Slot with progressIndicator". The Figma description is carried verbatim into the docs, with build notes.
  - **Verified:** all `var()` values exist in tokens.css, with no raw hex or px outside comments; types and lint pass; 7/7 story tests pass with a11y in error mode (the first run failed on a Vite dependency-optimisation fetch error and passed on re-run with no changes). Measured at 390px in Playwright: all six variants are 56px tall and the button positions match Figma.
  - **After a property-by-property comparison with Figma, you chose four fixes.** Three are applied as local overrides in `AppBar.module.css`, using the attribute-qualified technique from `resultBtm`'s `.skipButton`:
    - Icon box is 24px (`size.icon.300`) instead of ButtonIcon M's 20px.
    - Pressed turns the icon and the text-button label `text/secondary`.
    - The text button hugs its label: no 48px minimum width, no baseline nudge, Greed/Headline XXS Bold. **Accessibility trade:** the "Skip" tap target is about 30px wide.
  - **Re-measured:** Skip is 29.2px wide, its label centre, the ⋮ position and the Slot widths are within 1px of Figma, and a real mouse press gives `text/secondary` on both. Glyphs are closer but still differ, because Phosphor and Untitled draw differently.
  - **Not fixed:** letter spacing (Figma 1%, no token exists; skipped by your call, same as Button and TextBlock); Button Tertiary's loading spinner is `text/link` where Figma's appBar loading icon is `text/primary` (a Button-wide issue, by your call left and logged); AppBar doesn't expose disabled/loading on its buttons (by your call, a gap).

- 2026-09-14: **Skip while an answer is being judged goes to the next question.** Skip stays visible during processing (as in Figma's Thinking and Loading08 frames). Tapping it cancels the judging, throws the answer away, and logs the question as Skipped, so no verdict for it is shown. This matches how Close during processing already discards the answer. Closes the SPEC.md Open item.

- 2026-09-14: **Options, not decisions.** "Show me" hint animations for the exam plan, prototyped for comparison only. Nothing here is chosen or built into the app.
  - **Prototype:** `public/prototypes/show-me-hint/index.html`, a standalone HTML page over exported Figma frames (Exam00, the dimmed Hint-animate01 base, Turnon02). Private preview link: https://claude.ai/artifact/R2DHRWYxYu5Q9D4bM6VHMS (the published copy adds a fallback font stack and 16px side gutters, so it isn't exactly 390px wide on a phone).
  - **Option A, Knowie bounce** (Figma 13547:5824): Knowie rises under the Voice recall chip, two decaying hops (8px, 4px), then the chip pops (106% → 100% → 102% → 100%).
  - **Option B, lined arrow** (Figma 13651:2244): Knowie slides in from the left, then the looping arrow draws to the chip with its arrowhead last.
  - **Build notes worth keeping, whichever is picked:**
    - Knowie is `public/images/standby.svg` in a `mascotSlot` 2XL-style box.
    - The arrow path is copied from Figma vector 13651:2316, and its ARROW_LINES cap is rebuilt from the curve's end direction.
    - The chip pop uses a chip-shaped window onto the same background image. It only scales up, so it always covers the original, and it has no fill mode, so at rest it's pixel-identical.
    - Reduced motion fades in without movement.
    - Tokens are copied from tokens.css and checked against it.
    - Frame coordinates live in one labelled block; easing and timing are local, since there are no motion tokens.
  - **Open:** which option, whether B also gets the chip pop, whether the hint repeats if the chip isn't tapped, and timing on the test iPhone. Tracked in SPEC.md under "Options under consideration".

- 2026-09-14: **Build-plan review for SPEC.md screens 1–3** (verdict sheet, turn log, exam plan images). Decisions:
  - **Font on the phone is Inter.** Greed (`Greed Standard-TRIAL`) has no `@font-face` anywhere in the repo and only renders on the design Mac, where it's installed locally; on the test iPhone every screen would fall back to Safari's default serif. `font.family` in tokens.json moves to `Inter Variable`, loaded with `@fontsource-variable/inter` in both the app and Storybook. Regular stays regular, bold stays bold; the four weight tokens (400/600/700/900) keep their values. `next/font` rejected: it renames the family to a hashed name the token wouldn't match, and Storybook wouldn't load it. Consequences: tokens.json no longer matches Figma (still Greed); token names stay `--font-greed-*`; earlier pixel-parity measurements were taken in Greed and need re-measuring.
  - **The prototype runs as a home-screen web app on an iPhone 12/13/14/16e (390×844)**, so the exported 390×844 frames fit 1:1. Resolves SPEC.md Open #1, pending the spike confirming speech recognition works in that mode. Follow-ons: `viewport-fit=cover` plus a web app manifest; safe-area padding at the top of `Screen` and the bottom of sheets; a hidden facilitator entry to `/log` (triple-tap the Homescreen image's top-left corner), because a home-screen app has its own storage and no address bar; and a latency switch (normal / slow / hang) on `/log`, because `?latency=` can't be typed there.
  - **`Screen`'s mock status bar is hidden on the device** via a new `showStatusBar` prop (default `true`, for Figma/Storybook parity). On a real phone it doubled the real iOS status bar.
  - **Exam plan images are rendered bare, not inside `Screen`**, since each export already draws its own status bar, home indicator and tab bar. Tap zones live in `app/_prototype/` as fractions of the Figma frame (image geometry, not design values), each at least `space-1200` tall. `FrameImage` is prototype scaffolding, not a design-system component.
  - **Hint-animate01 is Figma 13547:5824** (Knowie under the Voice recall chip). 13657:3280 ("Hint-animateXX") was rejected: nothing on it points at the chip. Known: 13547:5824 is Option A's resting pose, so participants see Knowie in Option A's place although the animation is undecided; the node icons on this dimmed base are drawn tiny and off-centre in Figma itself; Knowie covers most of the "Biology" title.
  - **Leaving the loop goes to Turnon02, not Exam00:** close, Can't talk right now, and Continue from the summary. Exam00 shows the toggle off and the banner again, which reads as voice recall being switched off and costs three taps to get back. Only Don't Allow at the gate goes to Exam00, matching "Don't Allow snaps the toggle back off". SPEC.md to be updated.
  - **Turn log columns:** session (counts up each time a node opens, reset by Clear log), round (1 or 2), question (Q1–Q8), transcript, concepts hit (concept letters), outcome, latency (seconds from tapping Send to the sheet appearing), latency flag (normal / slow (random) / slow (forced) / hang), timestamp. **Outcomes** besides Pass / Partial / Fail / Silence / Skipped: Interrupted, Mic off, Left (idle), Left (judging), Tried typing. Cancel and the accidental quick tap still log nothing.
  - **Clear log confirms inline on the page**, not in a sheet: `Button` Secondary is invisible on a sheet's `background/surface` (logged 2026-09-11).
  - **A `unit` Vitest project** (node environment) is added next to `storybook`, for the session transitions, the turn log and later the keyword judge.

- 2026-09-14: **Step 0 of the screens 1–3 build done** (foundations shared by every screen).
  - **Inter:** `font.family.display` and `font.family.default` in tokens.json are now `Inter Variable`, loaded from `@fontsource-variable/inter` (new dependency) in `app/globals.css` and `.storybook/preview.tsx`. Checked in a real browser: every text node in all 98 stories computes to Inter Variable. A 390px truncation sweep found one regression. **Silence's title "Didn't catch that" ran 4px over (226/222px) and ended in an ellipsis. By your call it is now "Didn't catch it"** (fits at 190px), in `ResultBtm.tsx`, its Storybook docs and SPEC.md. Rejected: letting titles wrap, tightening the thumbs gap, a smaller title style. **Figma still says "Didn't catch that"; update it to match.** `TranscriptDisplay`'s "Sorry, I didn't catch that" is separate copy and fits, so it's unchanged.
  - **Layout and styles:** create-next-app leftovers are gone. Removed: the `#ffffff`/`#0a0a0a` variables, Arial, the Geist fonts and the "Create Next App" title. The body paints `background/page` with `text/primary` in Body M Regular, and `color-scheme` is dark. `viewport` sets `viewportFit: 'cover'` and `colorScheme: 'dark'`. `metadata.appleWebApp` sets `capable`, a title, and a `black-translucent` status bar so pages draw under iOS's real status bar. The new `app/manifest.ts` has `display: 'standalone'` and `start_url: '/'`, and deliberately no `background_color`/`theme_color`: both need a literal colour, and tokens only exist as CSS variables.
  - **`Screen`:** new `showStatusBar` prop (default `true`), with a `showStatusBar=false` story and a play test, plus notes in the story docs and design-system.md. The frame now pads by `env(safe-area-inset-top)` and `env(safe-area-inset-bottom)`. These are the device's own measurements, with no token for them, and they resolve to 0 in Storybook. Not yet covered: sheets in `bottomSheetOnly` are absolutely positioned to the frame's bottom edge, so `ResultBtm` and `BottomSheet` still need their own bottom inset (part of screen 1).
  - **Unit Vitest project:** a `unit` project in `vitest.config.ts` (node, `lib/**/*.test.ts`, `@` alias to the repo root). A throwaway probe test confirmed it runs and resolves `@/` imports. It exits 1 until `lib/` has tests (Vitest doesn't accept `passWithNoTests` inside a project config), which the turn log module fixes next.
  - **Verified:** all 98 story tests pass with a11y in `error` mode. `npm run lint` passes (0 errors; the two warnings were already there). `tsc` and `npm run build` pass. The built HTML carries the viewport, colour-scheme, apple-web-app and manifest tags. The changed CSS has no raw hex or px outside comments, and every `var()` exists in tokens.css.
  - Not yet re-measured: the earlier pixel-parity notes (AppBar, ResultBtm, TextField heights and widths) were measured in Greed.

- 2026-09-14: **Turn log data module built** (`lib/recall/types.ts`, `lib/recall/turnLog.ts`, `lib/recall/turnLog.test.ts`). No UI yet; `/log` comes after screen 1.
  - **Types:** `QuestionId` (Q1–Q8), `ConceptId` (A/B/C, as lettered in the content file), `Round` (1 | 2), `Verdict`, `TurnOutcome` (the three verdicts plus Silence, Skipped, Interrupted, Mic off, Left (idle), Left (judging), Tried typing), `LatencyFlag`. `toResultVariant()` maps Pass → `Success` and Fail → `Error` for `ResultBtm`.
  - **Storage:** the whole log is one JSON value under `voice-recall:turn-log:v1` in localStorage: the turns plus the last session number. `startSession()` returns the next number; Clear log removes the key, so numbering restarts at 1. The storage is passed in (`createTurnLog(getStorage)`), which is how the tests run in Node without a browser.
  - **Fails soft:** unreadable data reads as an empty log. A throwing or missing storage (Safari private mode, blocked site data, a full quota, server prerender) gives empty reads, and writes return `false`, so a broken log never breaks a student's session.
  - **For `/log`:** `getTurns()` returns the same array until the log changes, as `useSyncExternalStore` needs. It's cached against the raw stored string, so writes from outside the instance still show up. `subscribe()` and `getServerTurns()` complete the hook's contract.
  - **CSV:** header `session, round, question, transcript, concepts hit, outcome, latency (s), latency flag, timestamp`; CRLF line endings; RFC 4180 quoting (commas, quotes and line breaks in transcripts; concepts written "A, B"). Latency is seconds with one decimal; the timestamp is local time. Turns with nothing judged leave transcript, concepts and latency blank.
  - **Verified:** 16 unit tests pass (`npx vitest run --project=unit`); `tsc` and eslint are clean. To check the tests catch real bugs, CSV quoting and the stable-array cache were each broken on purpose: each break failed exactly its own test, then the code was restored.

- 2026-09-14: **Turn log revised after review.** Three decisions:
  1. **Giving up on the loading screen keeps its wait time.** For Skipped or Left (judging) during judging, latency is the time on the loading screen, from Send until the student tapped Skip or Close, with that turn's latency flag. The loading screen starts at Send, so this is the same clock as a normal turn's latency: it just stops when they give up instead of when the sheet appears. Turns that never reached Send (Skipped at idle, Left (idle), Interrupted, Mic off, Tried typing) leave latency blank.
  2. **Failed saves show on /log as "The last save failed".** The log counts failed saves in memory and also tries to write a small marker under `voice-recall:turn-log-save-failures:v1`. When only the big log write fails (a full quota), the marker still lands and the warning survives closing the app. When storage is fully blocked, only the in-memory count remains, which lasts as long as the app stays open. Clear log resets both.
  3. **Every node open is recorded.** `startSession(node)` writes a "Session started" row, so a session with no answers still appears. That covers leaving at the gate and closing the app. To support it, **every row gains a `node` column** (1 or 2), placed after `session`. The storage key moved to `v2` because the row shape changed (rows are now tagged session or turn).
  - **Verified:** 21 unit tests pass; `tsc` and eslint are clean. The failure marker and the session row were each broken on purpose: each break failed its own tests, then the code was restored.

- 2026-09-15: **Screen 1 (verdict sheet) built** as `VerdictScreen` (`app/screens/VerdictScreen.tsx`, Storybook `Screens/VerdictScreen`), the first screen in `app/screens/`. It composes `ResultBtm` over the loop screen as it looks right after Send. When screen 10 is built, this becomes its verdict state. Decisions from the review:
  - **No dim behind the verdict sheet**, matching the Figma frames. The transcript reads clearly, and close stays in reach, leaving the session for Turnon02. SPEC.md updated.
  - **Silence uses Figma 13659:3542**, which is a real `resultBtm` Silence instance, in place of the hand-drawn Silence03. Re-record shows Phosphor `Microphone`. That frame shows `TranscriptDisplay` Empty's copy ("I'm listening…") behind the sheet; **by your call nothing shows there when nothing was heard**, since "I'm listening" contradicts "Didn't catch it". Silence with words heard (a question, or the 15s timeout) still shows the transcript, which is a hard rule. The Figma frame still shows the Empty copy.
  - **Home indicator** (`reference/Errorwithhomebar.PNG`, the shipped app at 390×844pt): the sheet's top edge is at 678pt, the buttons at 754–810, and the home bar at 831–836. So the buttons end exactly the 34pt safe-area inset above the edge, with the home bar inside that space. `ResultBtm`'s bottom padding is now `max(space.700, env(safe-area-inset-bottom))`, 34pt on the phone. Earlier the same day it was 28 + inset, 62pt, which made the sheet 28pt taller than shipped. Storybook has no inset, so it keeps Figma's 28. No home bar is drawn in Storybook, since Figma's scaffold has none.
  - **Transcript text is Body S Regular (15/20px)**, per your Figma change to `transcriptDisplay` (13563:1612), in every state, replacing Headline XS Regular (18/20px). Matched in `TranscriptDisplay.module.css` and `design-system.md`. The set's Figma description still says Headline XS Regular, and text/secondary for Empty and Silence (they're text/tertiary). At 15px, Figma's own Overflow demo copy no longer overflows the 320px window, so the Overflow stories use a longer answer.
  - **A long transcript that ends in Silence** keeps its newest words behind the taller Silence sheet. It's rare, and left as it is by your call.
  - **"Didn't catch it" over a visible transcript** (a question, or the 15s timeout after words were heard) is accepted for this round, to watch for in the usability sessions.
  - **Transcripts are left-aligned in Figma too.** The centred-text overrides on Partial08b and Incorrect were removed in Figma.
  - **`transcriptDisplay` padding** (Figma 13563:1598, updated by you): every state pads `Space/800` on top, and Overflow is 32 + 320 + 96 = 448. Matched in `TranscriptDisplay.module.css`.
  - **Long transcripts keep the newest words visible, and the oldest fade out.** Overflow anchors its text to the bottom of the 320px window, and the top `space.1200` (48px, about two lines) fades from transparent with a CSS mask. This replaces the "there is no fade" note from 2026-09-14. Behind the verdict, short answers are Filled, switching to Overflow only once the text is taller than the window.
  - **Progress moves when a verdict appears**, counting the question just answered. Silence doesn't move it.
  - **XP chip stays a plain inline chip**, not `Chip` (SPEC.md Open #8 closed).
  - **Real question copy stays**, even when it wraps to 2–3 lines. Q2's copy was put into the bubbles of End09, Partial08b, Incorrect and Silence in Figma.
  - **Percentages are allowed for shape geometry** (an ellipse's `border-radius: 50%`, a `clip-path` triangle). They aren't design values. Added to the build-screen skill.
  - **Checked: the transcript is regular.** It renders Inter Variable at 400, 18/20px (Headline XS Regular); Inter just reads a little heavier than Greed at the same weight.
  - **Approved component changes done early:** `ButtonVoice` gained `leftIcon` (passed to `Button`), with mic and waveform on its Default and Recording stories. `ResultBtm`'s buttons pad by `env(safe-area-inset-bottom)` so they clear the home indicator. `BottomSheet` still needs the same for screens 5, 7 and 8.
  - **Built inline and logged in `component-gaps.md`:** the speech bubble with its tail and the mascot shadow, and the XP chip.
  - **Operational:** the Storybook MCP `test-run` reported stale passes after CSS and story edits in this session. A deliberately broken assertion still "passed" there but failed under `npx vitest run --project=storybook`. Verify story changes with the direct Vitest run until that's understood.

- 2026-09-15: **Screens renamed in Figma** (section "Design for voice recall first run experience"); node IDs unchanged except the gate. Old → new: Homescreen → **00Homescreen** (13619:3109), Exam00 → **01Exam** (13548:6324), Hint-animate01 → **02Hint-animate** (13547:5824), Turnon02 → **03VoicerecallON** (13548:6325), gate → **04full-screen gate (built)**, now the frame at 13575:1934 inside the section (the earlier 13555:8294, "Direction 1 — full-screen gate (built)", is still in the file outside it), Starting04 → **05Starting** (13548:6327), Talking05 → **06Talking** (13548:6328), Talking06 → **07KeepTalking** (13568:5231), Talking-finished07a → **08Talking-finished** (13568:5313), Loading/AnimateHowie08 → **09Loading/AnimateHowie** (13568:5451), End09 → **10End** (13568:5533), Partial08b → **Partial** (13568:5615). Unchanged: Incorrect, Silence (13659:3542), Thinking (13642:7889, outside the section), option B's Hint-animate01 (13651:2244), Silence03, Hint-animateXX.
  - Updated everywhere the current design is described: SPEC.md, design-system.md, the build-screen skill, the show-me-hint prototype's notes, the code (`FrameId` values, story names) and the frame exports, now `public/frames/00-homescreen.png`, `01-exam.png`, `02-hint-animate.png`, `03-voicerecall-on.png`. Story IDs changed with them.
  - **Not rewritten:** earlier entries in this log and `voice-recall-interview-2026-09-14.csv` keep the old names, since they record what was said at the time. Read them through this mapping.

- 2026-09-15: **The verdict sheet never covers the transcript.** `VerdictScreen` measures the sheet (its height changes with the variant and the phone's bottom inset) and stops the transcript area `space.600` above it. A long answer's Overflow is anchored to the bottom of that room, so the newest words always clear the sheet. When the room is shorter than the 320px window (the taller Silence sheet, or a longer question), the window shrinks to fit, so its fade stays at the top. Overflow's own 96px bottom padding is dropped behind the sheet. Measured: the newest line ends 32pt above both the Pass and Silence sheets. A new `outcome=Silence, long answer` story covers the taller sheet. The long-answer check fails with the fix removed (newest words 144pt and 248pt under the sheet). This replaces "a long transcript that ends in Silence stays behind the sheet".
- 2026-09-15: **02Hint-animate's node icons match 01Exam.** In Figma, the three `iconSlot` instances on 02Hint-animate were 12×12, left over in the top-left of each 54×54 slot; they were resized to 01Exam's 54×54, which also centres them. This fixes the "node icons drawn tiny and off-centre" noted on 2026-09-14. 03VoicerecallON's mics were already 54×54. `public/frames/02-hint-animate.png` was re-exported.

- 2026-09-15: **Transcript spacing updated to Figma again.** `transcriptDisplay`'s top padding is now `Space/600` (24) on every state, down from `Space/800`; Overflow is 24 + text + 96. Matched in `TranscriptDisplay.module.css`. Behind the verdict sheet, the transcript area adds no padding of its own above the text, as 10End, Partial and Incorrect show (the Silence frame's `Space/600` container padding belonged to its now-hidden transcript). The `space.600` gap above the sheet stays. Measured: short answers start 24pt below the disclaimer; long answers end 32pt above the sheet.
  - **Not matched:** Figma's Overflow variant is now top-aligned (`primaryAxisAlignItems: MIN`, with the 10End instance overridden to CENTER), which would clip a long answer's newest words. The build keeps the bottom anchor decided earlier the same day; Figma may want MAX back.
  - The 10pt growth in the Knowie block on these frames isn't a spacing change: the real Q2 question put into the bubble wraps to two lines.
- 2026-09-15: **The gate uses both frames in "Section 4"**, by your call. **13575:1934** ("full-screen gate (built)") is the gate on its own, and **13555:8294** ("04full-screen gate (built)") is the same gate with its "Allow microphone access?" sheet open. This reverses the 2026-09-14 decision to drop the sheet. (Corrected the same day: an earlier version of this entry called 13575:1934 unused, but since the sheet only opens after Turn on microphone, it's the gate's first state.)
  - **The sheet appears after Turn on microphone**, not on arrival, so the gate's own Can't talk right now stays one tap away.
  - **Allow** opens the real iOS prompt (Allow → loop, Don't Allow → 01Exam, as before).
  - **Don't allow closes the sheet**, back to the gate. Nothing is asked of iOS, so the real prompt stays available.
  - **Accepted:** a student who allows is asked twice in a row, by the sheet and then by iOS.
  - SPEC.md screen 6, Out of scope and end-to-end step 2 updated. **Knowie is `size="2XL"` in both states**, by your call, so it doesn't change size when the sheet opens. 13575:1934 still shows 3XL in Figma. Known: `Button` Secondary ("Don't allow") is hard to see on a sheet's `background/surface` (logged 2026-09-11).

- 2026-09-15: **Left padding added in Figma, matched in code.**
  - `aiDisclaimer` (13574:1784) now pads `Space/300` (12) on the left, alongside its `Space/100` top and `Space/800` bottom.
  - `transcriptDisplay` (13563:1612) pads `Space/300` on the left in all four states, so its text is 346pt wide, not 358.
  - Updated in `AiDisclaimer.module.css`, `TranscriptDisplay.module.css` (on Overflow's wrapper, so the 320px window keeps its height) and both components' story docs. Every screen using them picks it up.
  - The gate frames are now "04afull-screen gate (built)" (13555:8294, with the sheet) and "04bfull-screen gate (built)" (13575:1934, alone), directly in the first-run section. 04b's Knowie is now 2XL in Figma too. SPEC.md and the build-screen skill updated.

- 2026-09-15: **SPEC.md's Open list corrected.** Item 3 ("whether 00Homescreen is a static image like the exam plan") was still listed as undecided even though this was already answered on 2026-09-14 and built as part of screen 3 — marked ✅ Resolved rather than left stale.

- 2026-09-15: **Bottom-nav avatar swapped to Knowie, on your call, across all four exam plan frames** (00Homescreen, 01Exam, 02Hint-animate, 03VoicerecallON). Done in Figma directly, as a per-instance override on each screen's own `Avatar` instance (`Shape` rectangle's image fill), not a shared-master edit: `Avatar` (both the 00Homescreen set, 3248:81545, and the separate duplicate set the other three share, 9011:3309) is a generic profile-photo component with no icon/vector variant, and it's likely used elsewhere in the file outside this feature, so editing its master would have swapped the photo everywhere, not just these 4 screens. The new image is a rasterized export (PNG, 2×) of the real `standby` mascot instance already placed on 02Hint-animate, created with `figma.createImage` and applied via `scaleMode: 'FILL'` on each instance's own `Shape` fill. All 4 frames re-exported to `public/frames/` at 3× and verified in a real browser (390×844) against the running `ExamPlanScreen` stories — pixel match confirmed, `ExamPlanScreen.stories.tsx` still passes 5/5. `figma_execute`'s `fetch` to the local export receiver reported "Failed to fetch" on every call after the first, but the files landed correctly every time (confirmed by the receiver's own log and by file size/mtime) — the tool's success/error field can't be trusted for this step; check the written files instead.

- 2026-09-15: **Screen 4 (typing placeholder) built** as `TypingPlaceholderScreen` (`app/screens/TypingPlaceholderScreen.tsx`, Storybook `Screens/TypingPlaceholderScreen`). No Figma frame. Decisions from the review:
  - **No Skip button.** Removed at your direction: Back to voice is the screen's only action, and since it already leaves the session, nothing traps the student without a separate Skip. `ButtonGroup` dropped too (it requires exactly two children); the lone `Button` (Primary, L) sits alone in `bottomContent` and stretches full width on `Screen`'s default `align-items: stretch`.
  - **Back to voice now leaves for the exam plan (01Exam)**, not idle for the same question as SPEC.md originally described — your call, since typing has nothing to come back to. Caption copy updated to match ("Go back to the exam plan to try again by voice."). This narrows SPEC.md Open #6 to just the entry point (whether "Try typing instead" / "Type instead" route through this screen or skip straight to 01Exam); either way the final destination is now the same.
  - **12px (`size.space.300`) between the title and caption**, replacing `textBlock`'s own 4px gap at `variant="L"`. A local override (`.content .textBlock[data-variant='L']` in `TypingPlaceholderScreen.module.css`), out-ranking the component's own rule by ancestor class rather than editing `TextBlock` itself, since `TextBlock`'s tighter spacing is correct for its real usage (next to a mascot) and this screen has no mascot.
  - **Caption line height set to 150%** (`line-height: 1.5`), by explicit instruction. Recorded as a literal ratio, not a token substitution: no `line-height` token equals 150% of Headline XS Regular's 18px (27px; the nearest real token is `line-height/lg` at 28px).
  - **Verified:** both story tests pass (copy, no Skip button, the 12px gap and 27px line-height measured on the real computed styles, the button callback); each of the two new assertions was broken on purpose (gap and line-height reverted to the component's old values) and failed correctly, then restored. Full suite 119/119, lint and `tsc` clean, no raw hex/px, every token resolves. Measured at 390×844: no horizontal overflow, "Back to voice" spans the full 358pt content width.

- 2026-09-15: **Screen 5 (mic-off sheet) built** as `MicOffScreen` (`app/screens/MicOffScreen.tsx`, Storybook `Screens/MicOffScreen`). No Figma frame at build time. Decisions:
  - **Behind the sheet:** the loop's Idle look (mascot, question bubble, disclaimer, `TranscriptDisplay` Empty), reconstructed inline — same call `VerdictScreen` (screen 1) already made, since screen 10 isn't built yet and the tap never reaches Recording (the permission check fails first). No bottom action row behind the sheet, also matching `VerdictScreen`: it's hidden behind a modal sheet anyway.
  - **Dimmed** (`showBottomSheetBackground`), unlike the verdict sheet — that's the documented exception in SPEC.md's intro, not the rule.
  - **Built inline, not promoted:** the mascot/bubble/shadow and the XP chip are now duplicated a second time (screen 1 → screen 5); logged in component-gaps.md per the exception SPEC.md's screen 10 already carves out, not promoted to a component.
  - A reference mockup was then added to Figma ("Permission ask (invented — no system equivalent)", 13666:3837), and the build was revised against it on review:
    - **Restructured the sheet's content:** the title and Settings instructions now live in `BottomSheetAppBar` (`variant="withTitle"`, title "Your mic is off", caption "Open Settings, find Voice recall, then turn on Microphone."), not a separate `TextBlock` in `middleSection` as SPEC.md originally described — `middleSection` is unused. The reference showed this structure directly, and its caption text matched what had already been drafted, so this reads as confirming the original guess's copy while correcting the composition.
    - **Fixed: the Skip button was invisible against the sheet.** `ButtonIcon` Secondary's real fill is `background/surface`, the same colour as the sheet itself — the same known gap `ResultBtm` already hit and fixed for its own Skip button (2026-09-11 entry above). Same fix applied here: a local `.skipButton` override (in `MicOffScreen.module.css`) tints the inner circle to `interactive/secondary` + `interactive/onSecondary`, the same mechanism, not a change to `ButtonIcon` itself.
    - **`BottomSheetAppBar`'s `withTitle` inline padding raised to `space.600`** (24px), from the real master's own bound 16px — matches the reference mockup. Nothing else used `withTitle` before this screen, so this changed the shared component's default rather than needing a local override.
    - **`BottomSheet`'s `bottomSection` padding raised to `space.700`** (28px) on every side, from the real master's own bound 16px — matches the reference mockup and reconciles it with `ResultBtm`'s own `.bottomCta`, which already used this exact padding for the same kind of sheet action row. Also a shared-component change; nothing else had placed real buttons in that slot before this screen.
    - **`BottomSheetAppBar`'s title/caption changed from centered to left-aligned** (`.textSection`'s `align-items`, both text rules' `text-align`), confirmed against the reference mockup on a second review pass. Applies to every variant that shows text, not just `withTitle`.
    - **Not replicated:** a partial-width "Divider" instance sitting inside the reference mockup's own app-bar padding — read as an artifact of a rough, hand-built mockup rather than a deliberate element, so the sheet keeps its existing single top hairline.
  - **Verified:** all new/changed assertions (the sheet structure, the Skip button's tint, the left alignment) were each broken on purpose and failed correctly, then restored. Full suite 122/122 throughout, lint and `tsc` clean, no raw hex/px, every token resolves.

- 2026-09-15: **Screen 6 (gate) built** as `GateScreen` (`app/screens/GateScreen.tsx`, Storybook `Screens/GateScreen`). Figma **13575:1934** (gate alone) and **13555:8294** (gate with its permission sheet open), both in the first-run section; the third state, after an earlier denial, has no frame. Built from the frames, then revised across two further review passes:
  - **Initial build:** `TextBlock` (`variant="L"`) for the headline/body, `MascotSlot` (`size="2XL"`, `expression="approving"` — the frame's own bound Homie swap) peeking above a `ButtonGroup` (`variant="Vertical"`) of `Button` Primary **Turn on microphone** + Secondary **Can't talk right now**. The permission sheet reused screen 5's settled `BottomSheet` + `BottomSheetAppBar` (`variant="withTitle"`) composition. The mascot's ~41px overlap into the button stack is hand-placed in Figma, not a spacing-step multiple — the nearest token, `space.negative.600` (-24px), stands in. **Decision: the sheet dims the background** (`showBottomSheetBackground`) even though Figma's own "sheet open" frame has that off and its scrim hidden — nothing in SPEC.md exempts this sheet the way the verdict sheet is the one documented exception, so the general rule wins; flagged as possibly an authoring gap in Figma rather than a deliberate choice. **Not replicated:** a partial-width "Divider" inside the sheet's own app bar, the same artifact already seen on screen 5's reference mockup.
  - **First review pass, after you edited Figma directly:**
    - **`Button` Secondary/L's own master is now bound to `interactive/secondary`,** not `background/surface` — the known gap (`ResultBtm`'s own Secondary actions, 2026-09-11 entry above; screen 5's Skip button; this screen's own first-pass local tint) is fixed at the source, for size L only (S/M's masters are untouched). Changed in `Button.module.css` itself as a real default, not a local override, since it's now the master's real value — confirmed against the master component directly, not just the placed instance. Makes `ResultBtm`'s own inline `SECONDARY_COLOR` style redundant (same value); left alone, out of scope for this screen.
    - **Both button groups changed to `size="L"`** (the gate's own bottom buttons had been `size="M"`, matching the frame's real bound size at the time) — you changed the bound size in Figma directly.
    - **Text sizes stepped down, landing exactly on real tokens:** caption 18px Regular/24 → 15px Regular/20 (`font-greed-body-s-regular`, an exact match, replacing an earlier substitution). Title 44px Bold/44 (`textBlock`'s own default) → 33px Bold/36 (`font-greed-headline-l`).
    - **Both button groups' own gaps are hand-adjusted per placed instance,** not their master's real Vertical+L default (`space.200`, 8px, checked against the master `buttonGroup` component, 4893:5348): the gate's own is 12px (`space.300`), the sheet's is 16px (`space.400`) — different values, different instances.
  - **Second review pass:** title stepped down again, by explicit request, to **28px Bold/28** (`font-greed-headline-m`) — the same title-size change made to `TypingPlaceholderScreen` (screen 4).
  - **Third review pass, both bugs you caught in Storybook:**
    - **Fixed: the sheet's buttons weren't filling the sheet's width.** A wrapper `div` added around the sheet's `ButtonGroup` (solely to scope the 16px gap override above) was shrinking to content instead of stretching, since `BottomSheet`'s own `.bottomSection` centers children on the cross axis rather than stretching them — `ButtonGroup`'s own `width: 100%` then resolved against that shrunken box instead of the sheet. Fixed by giving the wrapper an explicit `width: 100%`, the same way the gate's own button wrapper already avoided this.
    - **Fixed: the title sat flush-left, not centered.** `textBlock`'s own `align-items: flex-start` left-aligns each child as a block, not just its text — `.content`'s `text-align: center` only centers text within a box, not the box itself. Masked at first because the caption wraps to 3 lines and claims the full width regardless of alignment, but the shorter title doesn't wrap, so it shrank to its own text width and sat flush against `textBlock`'s left edge. Fixed with a local `align-items: center` override on `textBlock`.
  - **Verified throughout:** every new/changed assertion (button size, both gaps, both text sizes, the fill-width fix, the centering fix, the Secondary/L contrast fix) was broken on purpose and failed correctly, then restored — including two that needed a genuinely independent reference (comparing a shrunken box to its own equally-shrunken wrapper doesn't catch a fill-width regression). Full suite 154/154 throughout, lint and `tsc` clean, no raw hex/px, every token resolves.

- 2026-09-15: **The running app now joins the built screens up** (`app/_prototype/PrototypeFlow.tsx`, `app/page.tsx`), rather than only being reachable one at a time through Storybook. Decided to build this now, once screens 1–6 existed, rather than waiting for every screen: an early join surfaces routing mistakes (see the Can't talk right now fix below) while the screen that caused them is still fresh, and gives you a real device/browser to review on, not just Storybook.
  - `PrototypeFlow` is a single client-side state machine, not real Next.js routes — the app runs as a home-screen web app with "leaving restarts" semantics and no address bar, so URLs beyond the one entry point and a `?screen=` review-link param aren't meaningful.
  - Every screen not yet built (the loop, the Why? sheet) stops at a temporary `NotBuiltScreen` with a way back to 03VoicerecallON, so the join-up stays truthful about what exists.
  - Mic permission (`lib/recall/micPermission.ts`) is asked for real via `getUserMedia` (open the stream, then immediately stop it — the only way a page can trigger the actual iOS prompt) and kept in memory only for the session; a reload starts over.
  - **Fixed while wiring this up:** the gate's own **Can't talk right now** had been routed to `03VoicerecallON` (the destination logged 2026-09-14 for the *loop's* own Can't talk right now button), rather than **01Exam**, which is what SPEC.md's screen 6 state table always specified for the gate's own button. Confirmed against Figma (node 13548:6324) that 01Exam is correct; fixed in `PrototypeFlow.tsx`. These are two different buttons on two different screens and shouldn't have been conflated.
  - **Decided (resolving SPEC.md Open #5):** a recheck (**I've turned on the mic**) that's still off shows an inline notice, "Still off — check Settings and try again.", under the Settings caption on the gate itself — `color/text-error`, same `body/S regular` size as the caption so it reads as a continuation of the copy rather than a separate alert. Chosen over a toast or re-showing the permission sheet (weighed as options, this was picked as the option that stays on the gate with no extra motion, and doesn't imply iOS's own prompt is being re-asked). Cleared on every node open so a stale notice can't reappear on a later visit. Implemented as a new `micStillOff` prop on `GateScreen`.
  - **Decided:** the mic's `unavailable` result (no `mediaDevices` at all — happens off HTTPS/localhost — or no mic hardware) is routed exactly like a real denial, not a separate path. It only shows up testing over plain HTTP; not worth designing a third gate state for.
  - **Verified:** both fixes broken and restored; full suite 168/168; lint and `tsc` clean; a real-browser Playwright pass confirmed the full path (denied → 01Exam, notice before/after a recheck, Can't talk right now → 01Exam, reopening the node shows no stale notice).

- 2026-09-15: **Screen 7 (Why? sheet) built** as `WhyScreen` (`app/screens/WhyScreen.tsx`, Storybook `Screens/WhyScreen`). No Figma frame for the screen itself. Reconstructs the same behind-the-sheet look as `VerdictScreen` (screen 1) — the loop, screen 10, still isn't built — with the Why sheet in place of `ResultBtm`. Wired into `PrototypeFlow` (Why? from the verdict view, Pass/Partial/Fail only; Got it → the loop's "Not built yet" stop) with three new review links.
  - **Initial build:** modelled on `reference/TapWhy?.PNG`. `BottomSheet` + `BottomSheetAppBar` (`Default`), explanation in `middleSection`; peeking `MascotSlot` and Got it (default `interactive/primary`) positioned outside `BottomSheet`, overlapping its top edge the same way the gate's mascot overlaps its buttons.
  - **Reviewed against `reference/Quiz-Why-explanation.png`** (a better layout reference, added the same day), across several passes:
    - **Sheet background wasn't actually transparent** — it was always `background/surface`; what looked wrong was the peeking mascot/Got it row painting *underneath* the sheet (DOM order, needed for the sheet-height measurement) instead of in front of it. Fixed by giving Got it its own `position: relative` so it paints above the sheet regardless of DOM order, while staying in normal flow so its height still counts toward the measured "don't cover the transcript" padding.
    - **Mascot's own overlap** widened from the gate's `space.negative.600` token to `calc(-1 * space.1200)` on request (closer to a third of the mascot covered, not an eighth), then **Knowie was hidden entirely, on your call** — Got it now floats clear above the sheet with a `space.100` (4px) gap instead of overlapping it, matching the reference exactly once the mascot's out of the picture.
    - **Got it's color:** tried `accent/coral/bold` first, corrected back to plain `interactive/primary` after checking the real `button` master (Figma 4871:29884, `size=M`) — then **overridden back to `accent/coral/bold` on your call**, a deliberate deviation from that master, as a local style override (same mechanism `ResultBtm`'s own `ACTION_COLOR` uses).
    - **Got it's size:** M → S, then measured against the real `size=S` master (Figma 4871:29852) on request — its padding-inline (space.300, 12px) already matched, but its height (a fixed 32px) didn't: `Button`'s shared `min-height` (space.1200, 48px) is a deliberate tap-target floor, right for `size="L"` (already documented) but wrong for this master. Fixed with a local override scoped to this one button, not a `Button.module.css` change — the app's other two `size="S"` buttons (`AppBar`'s text button, `Snackbar`'s secondary action) are text-only Tertiary buttons where the 48px floor may be the deliberate accessible target, flagged rather than touched.
    - **Sheet's own header:** replaced `BottomSheetAppBar` with a plain inline handle (same token values as the real one) — `Default`'s only no-title variant still reserves a 64px zone sized for its titled siblings, which was most of "a lot of padding at the top" you flagged. Paired with rebalancing the explanation's own top/bottom padding, both sides now total `space.2000` (40px) from the sheet's edges to the visible text.
  - **Deferred, not fixed:** a `color-contrast` axe failure on `SummaryScreen`'s `.rowSkipped` (screen 8, built separately) — `text/tertiary` measures 4.34:1 against `background/surface`, under the 4.5:1 AA minimum. On your call, left as-is: that text is supplemental, not normal body copy, so the strict AA minimum doesn't apply the same way. `text/secondary` (~7.2:1) was the identified fix if this changes.
  - **Verified throughout:** every new/changed assertion (bold-concept mapping in both the screen's own stories and `PrototypeFlow`'s wiring, the sheet-height/overflow-clearance fix, Got it's paint-order fix, the Got it→next-question routing) broken and restored. Full suite 173/173 → 141/141 (once screen 8's own unrelated failure entered the tree) throughout, lint and `tsc` clean, no raw hex/px outside comments.

- 2026-09-15: **Screen 8 (summary) rebuilt to match `reference/Finish-quiz.png`**, on your instruction, replacing the row-based/Try-again design this same day's earlier build (and the 2026-09-14 interview's own "Summary" notes above) had used. You then supplied the real Figma frame behind that reference (node `7366:69693`, `scaffold`/`size=iPhone 13`, "Design the core flow" page); the rebuild was matched against it directly (colours, exact font sizes/weights/line-heights pulled via `figma_execute`, the REST token having expired) rather than against the PNG alone.
  - **Composition:** a full screen, not a sheet over the loop — `MascotSlot` (`size="3XL"`) above a headline/subhead pair, a two-card stat row (`XP`, `Score`), and `ButtonGroup` (Horizontal, L) of `Button` Secondary **Share** + `Button` Primary **Claim XP**. Headline/subhead/mascot expression are dynamic on pass count (see SPEC.md screen 8 for the exact copy branches); a clean run reuses Figma's own copy verbatim, the other two branches are authored copy not in any content file.
  - **Dropped from the same-day earlier build:** the per-question row list (question, transcript snippet, verdict) and the Try again action. This is the decision screen 10's own entry below treats as final, not an oversight — a session is one pass through 4 questions, no reruns.
  - **Dropped from the Figma frame itself:** the third stat card, "Blazing" (a stopwatch icon + elapsed session time, e.g. "2:09"). Nothing in this mocked session tracked elapsed time, the same fabricated-metric problem already ruled out for the static XP chip. **A real version (derived from the turn log's own session-start/turn timestamps) was scoped in full** — `formatDuration`, a standalone `sessionDurationSeconds(rows, session)`, a third `StatChip`, mock wiring for the review links — **and your call was to leave the screen as built rather than add it.** The plan is on record at the time of this entry but wasn't implemented.
  - **Close (top-left X) is a deliberate addition, not in the Figma frame at all** (its own top nav is empty). Since Share and Claim XP are both decorative this sprint and neither leaves the screen, Close is the only way out — the hard rule against trapping the student with no way forward.
  - **Share and Claim XP are present but unwired**, per your instruction ("don't trigger anything") — `onShare`/`onClaimXp` are left unset by `PrototypeFlow`, so neither does anything; this is enforced at the wiring level, not inside `SummaryScreen` itself, which still fires whatever handler it's given.
  - **Fixed in `BottomSheet` while testing the (now-abandoned) row-based build, kept because it's a real bug:** `.middleSection` failed the `scrollable-region-focusable` axe rule once its content actually overflowed and scrolled — no prior consumer's content had overflowed, so this never fired before. Fixed with `tabIndex={0}` on that wrapper; benefits every `BottomSheet` consumer, not just this screen.
  - **Resolves the "Deferred, not fixed" `.rowSkipped` contrast note in the Why? sheet entry above:** that element no longer exists — the row list it belonged to was removed in this rebuild, not fixed in place.
  - **Verified:** 14/14 story assertions (`AllPass`, `SomeNonPass`, `NonePass`, `AfterTryAgain`), full suite 145/145 (no regressions from the `BottomSheet` fix), `tsc`/lint/build clean, no raw hex/px outside two logged, explained exceptions (two off-system condensed display font sizes with no matching token, same pattern as `ResultBtm`'s own Error title). Stat chip's inline pattern logged in `component-gaps.md`.

- 2026-09-15: **Screen 10 (the voice recall loop) built** as `LoopScreen` (`app/screens/LoopScreen.tsx`, Storybook `Screens/LoopScreen`), the last major screen, then wired into `PrototypeFlow` for a real multi-question session. Three decisions made going in, from a planning pass before any code:
  - **Real STT deferred; built against a scripted stand-in.** Verification step 0's spike (real mic + `webkitSpeechRecognition` on the test iPhone) still hasn't run — rather than wait, this screen was built now against `lib/recall/scriptedTranscript.ts`, written to the same shape (`start`/`stop`, interim + final callbacks) a real recognizer wrapper will need, so the eventual swap is a small follow-up, not a rebuild. SPEC.md's own screen 10 and Verification §0 entries updated to say so, so they stop reading as unbuilt/blocked.
  - **Try again is out, for real.** `SummaryScreen`'s own 2026-09-15 rebuild (matching `reference/Finish-quiz.png`) already dropped Try again — no button, no rerun rows, no caption — and this build treats that rebuild as the actual decision rather than an oversight against SPEC.md's older "two rounds per question" text. A session is one pass through a node's 4 questions, then Summary, then Close; `round` stays `1` in every turn-log row (schema untouched, to avoid an unrelated migration). SPEC.md's Session rules and Open item 12 updated to match.
  - **The fake transcript source is facilitator-controlled and streams live**, not silent/instant: a "Next answer" radiogroup on `/log` (`app/screens/LogScreen.tsx`, `lib/recall/scriptedTranscript.ts`'s `getScriptedAnswer`/`setScriptedAnswer`/`subscribeScriptedAnswer` — same persisted-override pattern as the existing latency switch) picks Pass/Partial/Fail/list/question/blank; on Start it streams word-by-word into `TranscriptDisplay` at a natural pace (`startScriptedSpeech`, 220ms/word), so Still listening, Overflow-anchoring and cancel-before-send are all genuinely reviewable now, not deferred along with real STT. Never visible to a student, same as the latency switch.
  - **New `lib/recall/` modules, judge and timing logic that didn't exist before:** `questions.ts` (all 8 questions transcribed from `content/voice-recall-questions.md` into structured concepts/explanation/samples — the one source both the judge and the scripted source read from, replacing `PrototypeFlow`'s old hand-rolled `Q1`/`Q2`/`Q2_EXPLANATION`/`WHY_MISSING`), `judge.ts` (`isQuestion`, `isRealSentence`, `judge` implementing every "On Send" rule; 37 tests, one per rule plus every sample answer for all 8 questions), `processingLatency.ts` (`planProcessing`: normal 2–4s, ~1-in-5 random-slow 7–8s, facilitator forced slow/hang, hang = no verdict ever).
  - **One content fix found while writing `judge.test.ts`:** Q8's concept B (in `content/voice-recall-questions.md`) lists "inside the cell" as a phrase, but including it made the Q8 Partial sample ("It's a liquid inside the cell.") register 2 concepts + a real sentence → Pass instead of the intended Partial. `lib/recall/questions.ts` omits that one phrase from Q8's concept B; the source markdown file itself wasn't touched. Matches Open item 13's own flag that Q8's concept B needs checking.
  - **Interpretations made wiring `PrototypeFlow`, none in SPEC.md's own text:**
    - **Close mid-Processing logs `'Left (judging)'`.** `LoopScreen`'s `onClose` was extended to also pass `latencyMs`/`flag` when `phase === 'processing'` (same shape `onSkipProcessing` already had) so this turn's elapsed judging time is captured, matching `TurnRow`'s own doc comment ("until the sheet appeared, or until the student gave up (Skipped or Left (judging) while judging)") rather than logging it as `null`.
    - **Can't talk right now (loop, idle only) logs `'Left (idle)'`** — the same outcome a bare idle Close would log — since both are "gave up before Send" from the student's side, and `TurnOutcome` has no separate value for it.
    - **Close mid-Recording logs nothing** (per `LoopScreen`'s own doc comment: treated like Cancel, nothing committed yet) — matches Verification step 8's "Can't talk right now on Q2 → 01Exam; reopening starts at Q1" expectation of a clean restart with no stray row.
    - **Silence's own Skip and Re-record don't write a second turn row** — the Silence outcome was already logged the moment `onSilence` fired; Skip just advances the session (`'Skipped'`, session bookkeeping only) and Re-record just returns to the loop without advancing (same question retried).
  - **`NotBuiltScreen` deleted** (`app/_prototype/NotBuiltScreen.tsx` + its module.css), along with the `'not-built'` `PrototypeView` variant it backed — its own doc comment said "delete each use as its real screen lands," and the loop was the last screen still using it.
  - **`PrototypeFlow.tsx` rewritten** for a real 4-question session per node: `Session`/`LastTurn` state (`app/_prototype/reviewScreens.ts`), `advance()` moves to the next question or the summary after the 4th, every outcome calls `turnLog.appendTurn(...)` with the real question/transcript/concepts, and `VerdictScreen`/`WhyScreen` read their content from `questions.ts` via `lastTurn` instead of the old hand-rolled samples. `LoopScreen`'s own Start does a real `requestMic` recheck before Recording; a denial shows the mic-off sheet over the loop instead.
  - **Verified:** `npx tsc --noEmit`, `npm run lint` (0 errors, the 2 pre-existing unrelated warnings only), `npm run build`, and the full `npx vitest run --project=storybook` suite (155/155 across 34 files) all clean; `lib/recall`'s own unit suite (84/84: judge, scriptedTranscript, processingLatency, turnLog, micPermission). No raw hex/px in new `*.module.css` (one motion-only `translateY(-4px)` in the mascot's thinking-loop keyframe is a bespoke animation offset, not a spacing/size value, so it isn't a token substitution candidate). Component gaps logged: the loop's own 4th copy of the question-bubble/mascot-shadow/XP-chip trio, the mascot thinking-loop animation (no Storybook component covers motion), and the "Next answer" control (same gap as the latency switch).

- 2026-09-16: **"Show me hint animation" (SPEC.md, Options under consideration) closed: Option A, Knowie bounce.** Two decisions, both yours. (1) Build it for real rather than leave it in the standalone comparison prototype (`public/prototypes/show-me-hint/index.html`, still there, untouched, for the record). (2) Scope it to just Knowie, composited live over 02Hint-animate's still export, rather than rebuild the whole exam plan frame from components — SPEC.md's "the exam plan is out of scope as components" stands for the banner, chip, node icons, tab bar and status bar; only the animated element is real.
- **02Hint-animate re-exported from Figma (13547:5824) with the `mascotSlot` instance (13547:5885) hidden for the export, then made visible again** — `public/frames/02-hint-animate.png` no longer has Knowie baked in, so the live overlay is the only Knowie on the frame. Screenshotted and diffed against the prior export first: identical except Knowie's absence. Figma's own master is untouched (visibility was toggled off, exported, then restored).
- **`ExamPlanScreen.tsx` composites a real `MascotSlot` (`size="2XL"`, `expression="standby"`) plus a chip-pop overlay div** at Figma's own mascotSlot (129, 95) and chip (119, 56, 129×40) positions on that frame — frame geometry, not design values, the same exemption `frames.ts`'s tap-zone boxes already use. `FrameImage` gained an optional `children` prop so the overlay lives inside the same `hidden`-toggled wrapper as the image: since `hidden` is the native HTML attribute (`display: none`), the animation restarts every time `ExamPlanFlow` shows this frame again, with no extra state needed.
- **Ported, not re-derived, from the Option A build already worked out in the standalone prototype:** the rise (420ms, custom ease-out-expo, 160ms delay), the two decaying hops (8px then 4px, each a barely-there squash, split onto a nested element from the rise so the two transforms compose instead of overwriting each other), and the chip-pop (a chip-sized crop of the same base image, scaled 106% → 100% → 102% → 100% so it's pixel-identical to the chip underneath except while popping). The cubic-beziers and hop offsets have no motion token in this system — same gap already flagged for the loop's Processing bob — so they're inline in `ExamPlanScreen.module.css`, documented as such.
- **Reduced motion follows the app's own convention, not the standalone prototype's.** The prototype opts out of motion under `prefers-reduced-motion: reduce`; `LoopScreen`'s Processing bob instead opts in under `prefers-reduced-motion: no-preference`, defaulting to fully visible and static. This build matches `LoopScreen`: Knowie is always shown, motion is only added when there's no reduced-motion preference.
- **Story assertion note:** `HintAnimate`'s play function asserts the Knowie `<img>` is `toBeInTheDocument()`, not `toBeVisible()` — the entrance animation legitimately holds it at `opacity: 0` for its first 160ms under real animation timing in the browser test runner, so asserting visibility immediately after mount failed for the right reason, not a bug. Confirmed the assertion still catches a real regression (temporarily removed the overlay, watched the test fail, restored it).
- Full `npx vitest run --project=storybook` suite verified clean after this change (154/154 across 34 files); `npx tsc --noEmit` and `npm run lint` clean (same 2 pre-existing unrelated warnings only).

- 2026-09-16: **Correction to the entry above, per your instruction ("only animate once"): the Knowie bounce was replaying on every revisit, not just within one `ExamPlanFlow` mount as the previous entry assumed.** Two fixes, in order, because the first one didn't actually reach the real bug.
- **Attempt 1 (incomplete):** gated the animate-* classes behind component-local state (`isFirstShow`, computed at render time only — reading a ref during render to do this instead trips `eslint-plugin-react-hooks`'s `react-hooks/refs` rule, so a `useState` flipped by a `setTimeout` a fixed ~2000ms after reveal is used instead, timed to land after the animation has already finished on its own so the class swap is visually a no-op, not a cut). This is correct for repeats *within one `ExamPlanFlow` mount*, but `ExamPlanFlow`'s own tap zones are one-way (00Homescreen → 01Exam → 02Hint-animate → 03VoicerecallON, nothing goes back), so that mount can only ever show 02Hint-animate once anyway — the fix was solving a case that can't happen.
- **The real bug:** `PrototypeFlow.tsx`'s `switch (view.screen)` returns a different component type per screen, so leaving `'exam-plan'` (the gate's Don't Allow/denial and the loop's Can't talk right now both exit to 01Exam) fully unmounts `ExamPlanFlow`, and returning to it mounts a *fresh* instance — resetting any state kept inside that tree. Confirmed by driving the real app end to end in Playwright (`node --use-fake-ui...` was unnecessary; overriding `navigator.mediaDevices.getUserMedia` to reject was enough to simulate a real denial): Show me → 02Hint-animate (animates, confirmed) → Voice recall → node → gate → Allow → denied → back on 01Exam → Show me again → 02Hint-animate replayed the whole bounce and chip-pop.
- **Attempt 2 (fixed, verified the same way):** `hintPlayed`/`onHintPlayed` moved out of `ExamPlanScreen.tsx` entirely and into `PrototypeFlow`'s own state (`app/_prototype/PrototypeFlow.tsx`), which never unmounts across screen switches — the same category of state `micStillOff` already models ("cleared on every node open, so a stale notice can't reappear on a later visit"). Threaded through as props: `PrototypeFlow` → `ExamPlanFlow` → `ExamPlanScreen`'s `frameOverlay`. Both default to `false`/a no-op so the other stories, which don't touch this frame, need no changes. Re-ran the exact Playwright script after the fix: second reveal now shows Knowie at `opacity: 1` with neither the `rising` class nor the chip-pop element present — no replay.
- Resets on a full page reload (a fresh `PrototypeFlow` mount), same as everything else in this prototype (turn log excepted, which persists to the device on purpose) — not persisted to `localStorage`, since nothing else in this session state is either and a reload already restarts the whole exam plan from 00Homescreen.
- Full `npx vitest run --project=storybook` (154/154), `npx tsc --noEmit`, and `npm run lint` all clean after this fix.

- 2026-09-16: **New prototype, not built into the app: `public/prototypes/hint-arrow/index.html`, animating Option B's purple line/arrow (Figma `Hint-animate01`, 13651:2244, Vector 1 at 13651:2316) into a self-drawing stroke ending in an arrowhead at the Voice recall chip.** Per your instruction, plays once (page load), no auto-loop; a manual Replay button re-triggers it for review.
- **The live Figma frame no longer matches this sprint's earlier notes about Option B.** The 2026-09-14 log entry (and the standalone `show-me-hint` comparison it fed) describes Option B as "Knowie slides in, then the arrow draws to the chip" with a `mascotSlot` at (19, 145) — but the current live frame has no mascotSlot instance at all, only the one arrow vector. Built to match the live file (this project's standing rule), i.e. no Knowie slide-in: just the line and its arrowhead, matching what you asked for.
- **The curve is a real transcription, not a redraw.** Read `Vector 1`'s actual `vectorNetwork` off the live file via the Figma plugin bridge (5 vertices, 4 cubic-bezier segments, stroke `#9178E6` at 6px — exactly `--color-mascot-body`/`violet-500`) and converted its tangent handles into SVG cubic-bezier control points directly, rather than eyeballing a similar-looking squiggle. Positioned and rotated into the 390×844 frame with Figma's own `relativeTransform` (34.48° rotation) recreated as a single CSS `matrix()`, not approximated.
- **The arrowhead is the one hand-built approximation, flagged as such in the page itself.** Figma's last vertex uses the `ARROW_LINES` end-cap style, which is rendered internally by Figma (confirmed by the "Cannot unwrap symbol" error trying to read `strokeCap` directly — it's `figma.mixed`, a Symbol, on this path) and isn't exposed as editable path data through the plugin API. Exporting the vector as `SVG_STRING` does bake in the real cap geometry, but as one merged *filled* compound path, which can't be stroke-dash-animated for the draw effect — so the line itself uses the real stroked centerline (works with `pathLength`/`stroke-dashoffset`), and the arrowhead is a separate two-stroke open chevron, angled off the curve's own real exit tangent, sized to the same 6px stroke. Not pixel-identical to Figma's native cap, unlike the curve.
- **Line-drawing technique per the animate skill's `svg-animation.md`:** `pathLength="100"` on all three paths (the curve, plus the two arrowhead strokes) normalizes dash math regardless of real path length; `stroke-dasharray: 100 101` (gap slightly past the dash so the round cap doesn't peek through at rest); animate `stroke-dashoffset` from 100 to 0. Curve draws over 720ms (custom ease-in-out-cubic, matching the "moving while already on screen" pick), arrowhead strokes draw in a quick, staggered 160ms right as the curve finishes, both ported from the already-considered timing in the old `show-me-hint` comparison (Option B's own "About 1.3s" estimate, SPEC.md's Options table) rather than re-timed from scratch.
- **Reduced motion bug found and fixed before shipping this page:** the first pass gated only the *animation* behind `prefers-reduced-motion: no-preference` but left the base `stroke-dashoffset: 100` (hidden) outside it — so under reduced motion the arrow never drew and never appeared at all. Fixed by flipping the default to drawn (`dashoffset: 0`) and only hiding it, to then animate back in, when motion is allowed — the same "gentler, not hidden" shape this session's other reduced-motion work already follows. Caught by testing both `reducedMotion: 'reduce'` and default in Playwright before calling it done, not just eyeballing the motion path.
- Background reuses this session's `public/frames/02-hint-animate.png` (the Knowie-hidden re-export made for Option A above) rather than the older, lower-resolution `show-me-hint/hint-base.png`: both Figma frames share the identical `scaffold` structure and sizing (checked node-for-node), so the same dimmed, mascot-free backdrop is correct under either overlay, and this one is a fresher, higher-res (3×) export.
- Not integrated into Storybook or `ExamPlanScreen` — a standalone comparison page only, same status as the existing `show-me-hint` prototype it sits alongside. No decision between Option A (already built and shipped, above) and this refreshed Option B has been made or asked for.

- 2026-09-16: **Your call, after seeing both built: Option B (lined arrow), not Option A.** 02Hint-animate's live overlay was rebuilt to replace the Knowie bounce with the purple line/arrow, porting the already-verified `public/prototypes/hint-arrow/index.html` build (curve, arrowhead, timings, reduced-motion handling — all unchanged) into `ExamPlanScreen.tsx`/`ExamPlanScreen.module.css` in place of the `MascotSlot` overlay. `public/frames/02-hint-animate.png` needed no change (Option B has no mascot either way, so the same Knowie-hidden export from the Option A build is still the correct background). `SPEC.md`'s screen 3 section and "Options under consideration" table updated to match; `component-gaps.md`'s hint-animation line updated in place (one gap, its fill changed, not two entries) — see both files' own 2026-09-16 notes for what's now stale about the Option B table row this replaces (no Knowie slide-in on the live frame, contradicting that table's own description, already flagged when `hint-arrow` was built).
- **The exact same "animate once" state machine carries over unchanged** — `hintPlayed`/`onHintPlayed`, still owned by `PrototypeFlow` for the reasons logged above, just now gating a `.drawing` class on three `<path>`s instead of `.rising`/`.hopping` on a `MascotSlot`. `HINT_ANIMATION_MS` shortened from 2000 to 1300 to match Option B's shorter ~1.1s sequence (line ends 920ms, arrowhead strokes end 1060/1100ms) rather than reusing Option A's number.
- **A real bug found while verifying this build, not present in the standalone `hint-arrow` page:** the reveal was a complete no-op the first time it ran here — `getComputedStyle` showed `stroke-dashoffset: 0px` at every timepoint, including mid-animation samples taken by pausing the Web Animations API and setting `currentTime` directly (ruling out a page-load timing race before suspecting anything else). Root cause: `.arrowBox path { stroke-dashoffset: 0; }` (specificity 0,1,1, the reduced-motion-safe base value) out-ranked `.drawing { stroke-dashoffset: 100; }` (specificity 0,1,0) regardless of source order, so the *underlying* (pre-animation) value the browser computed for the `to`-only `@keyframes draw` rule's implicit "from" was already 0 — the animation was genuinely interpolating 0 → 0. `animationName` and `playState` both read as correct throughout, which is what made this worth recording: those two look like confirmation the animation is working right up until you check the actual value it's producing. Fixed by qualifying the rule as `path.drawing` (matching `.arrowBox path`'s own specificity, and appearing later in the file, so source order now decides it in `.drawing`'s favor) — the identical fix, for the identical reason, already logged in this file for `GateScreen`'s `.sheetButtons div[...]` and `ResultBtm`'s `.skipButton`. Verified via the same Web Animations API technique: mid-animation `stroke-dashoffset` now reads a real interpolated value (48.3px at the line's own midpoint) instead of a flat 0.
- Re-verified end to end after the fix, the same way the Option A replay fix was verified: real Playwright run against the actual app (not just Storybook), `navigator.mediaDevices.getUserMedia` overridden to reject so **Allow** behaves like a genuine device denial. Reveal #1 carries the `drawing` class on all three paths and settles to a fully-drawn `0px` after ~1.5s; the gate/denial round trip back to 01Exam and a second **Show me** tap (reveal #2) shows all three paths with no `drawing` class and already at `0px` — drawn, static, no replay. Reduced motion checked separately (`reducedMotion: 'reduce'`): fully drawn immediately, no wait needed.
- Full `npx vitest run --project=storybook` (154/154), `npx tsc --noEmit`, and `npm run lint` all clean after this rebuild. `HintAnimate`'s story assertion updated from checking for Knowie's `<img>` to `canvasElement.querySelectorAll('svg path')` having length 3 (the curve plus both arrowhead strokes) — same break-it-to-confirm-it-catches-a-regression check run again on the new assertion.
- `public/prototypes/hint-arrow/index.html` and `public/prototypes/show-me-hint/index.html` both left as they are — standalone comparison prototypes, not the source of truth now that the real build exists in `ExamPlanScreen`.

## Not building
- Auto-endpointing or continuous listening
- Tutoring or open conversation branch if the student asks Knowie something
- A custom speech-to-text engine or real judging (the browser's built-in recognizer is used for transcripts; judging is a mocked keyword judge)
- Hints or a hint ladder, and Say it back
- Resume mid-session (leaving restarts)
- The typing turn (this sprint; text-fallback buttons lead to a placeholder)
- Review node and review queue (this sprint)
- Exam plan screens as real components (static images this sprint)
- In-loop snackbars (the only snackbar is the exam plan's readiness banner)
- XP (this sprint)
- A confidence rating before or after answering
- VoiceOver and reduced-motion handling for the live transcript and waveform (this sprint)
- Thumbs up/down feedback on the verdict (this sprint)
- A chat input ("How can I help?") on the Why? explanation sheet

## Leftover from setup (not yet cleaned up)
- `public/next.svg`, `vercel.svg`, `window.svg`, `globe.svg`, `file.svg` are unused create-next-app defaults
- `README.md` is still the unedited create-next-app default
